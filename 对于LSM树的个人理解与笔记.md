#### 对于LSM树的个人理解与笔记



![855aec97b32ec86733d15da7a7a7744d](C:\Users\msduser\Desktop\学习笔记\assets\855aec97b32ec86733d15da7a7a7744d-1744694581816-2.png)





LSM树（Log-Structured Merge Tree）是一种专门针对大量写操作做了优化的数据存储结构，尤其适用于现代大规模数据处理系统，如NoSQL数据库（如Cassandra、HBase、RocksDB等）和键值存储。尽管其名称中包含“树”，但它并不直接对应于传统的树状数据结构，而是指一种数据管理策略或体系架构。

LSM为什么会出现：

当数据量大了之后，读操作采用顺序遍历来进行查找肯能是不行的，性能太低了。所以需要维护一种数据结构用来帮助提升读的效率，在关系型数据库中用B+树（索引）来维护数据的关系，便于查找。

关系型数据库中对B+树的使用在读的时候性能不错，但是在写的时候存在明显的性能问题。不是说B+树这种数据结构在写的时候存在性能问题，而是关系型数据库中是将树结构存在磁盘上的，并且树的节点在磁盘上的存储是分散的，数据的存储也是分散的，这种落地方式在面对写操作的时候会有性能瓶颈。

原因如下：

首先是写操作。写操作是容易引起B+树的结构的调整的，要调整树的结构当然要去读写树的节点，树的整个结构都存在磁盘上的，所以要走磁盘IO，调整树当然就要去对磁盘上存的树的节点进行读写，B+树在磁盘中的存储是分散的，所以这里的IO是随机IO。写数据的时候，数据也不是顺序存放的，也是分散存放的，也会是随机IO。

其次是读操作，即使B+树尽力优化了树的层高，减少了磁盘IO次数，但是毕竟树的节点和数据不是顺序写入进行存储的，所以在访问的时候还是会进行随机IO，在关系型数据库的场景下倒是没什么问题，在大数据场景下要读的数据量是海量的，海量数据都是进行随机IO的读，性能上来说也是不佳的。

所以在海量数据的写入的时候B+树不是一个优质的选择。对着大数据场景的出现，LSM树出现，用于专门应对海量数据的写入。

总结一下B+树面对海量数据无力是因为：

树存在磁盘上，读写都是磁盘IO

树是分散存放的，读写都是随机IO

数据是分散存放的，读写都是随机IO

LSM树其实就是一套打法，核心目的就是为了规避上面的问题。

LSM树会将树结构放在内存中，从而规避磁盘IO，当然内存是有限的，到了一定条件后会将当前内存中这个版本的树存到磁盘中，存磁盘的时候开辟一块连续空间，将树的节点连续存储在一起，然后刷新内存再重新开始存新进来的内容。读的时候就会先去读内存，内存中没有再去读磁盘。由于磁盘中树的节点是连续写在一起的，会减少随机IO。

当在落磁盘的时候，磁盘上如果有历史版本的话，会和最新的历史版本进行合并。也就是说越新的历史版本，树越”茂盛“：



2.LSM树的落地实现
LSM树的落地实现通常包含内存中的MemTable（内存表）和磁盘上的SSTable（Sorted String Table，有序字符串表）两部分。

数据首先写入内存中的MemTable，数据在memtable中就会被组织成平衡二叉树：



当MemTable达到一定大小时，会被转换为不可变的SSTable并刷写到磁盘，写入磁盘的时候会开辟一段连续的存储空间，将树的内容连续存储在一起：



除了上面的内容外，还有一个核心内容——Compaction，合并。

由于肯定会落多次磁盘，生成多个版本的sstable，会浪费磁盘空间，所以还会存在合并操作，将多棵小树合成一棵大树。合并的时机一般有两个：

一个时机是在落磁盘生成新的sstable的时候会和之前最新的历史版本对应的sstable进行一次合并，两棵小树合并出一棵大树来。另一个时机是磁盘的存储达到一定阈值之后多个历史版本的sstable会进行合并合并出一棵大树来。

还有最后一个问题就是如何删除LSM树中的元素？

在memtable中删除了，但是sstable中还有，直接删除是没有用的，下次合并的时候还是会把已经删除的元素合并进来。所以LSM的做法是给要删除的元素打上一个墓碑标记，墓碑标记用来标记数据被删除了，下次合并的时候就能通过墓碑标记来判断哪些元素不用合并进来。


#### 内存中的**虚拟分片**（Virtual Shard）：



## Virtual Shard（虚拟分片）= 间接映射（就像指针）

你可以把 **Virtual Shard** 理解为一个 **“指针”或“中间层映射”**，它本身不存储真实数据，而是 **指向真实数据的存储位置**。

你的类比非常贴切，比如在 C 语言中：

```
cCopyEditint real_data = 42;
int *virtual_shard = &real_data;
```

Virtual Shard 就像 `*virtual_shard`，不是数据本身，而是一个 **指向真实位置的中介映射**。

------

## 🔁 数据查找流程：两层映射

### ❌ 没有 Virtual Shard 的情况：

```
textCopyEditkey → 节点A
key2 → 节点B

→ 如果增加或删除节点，几乎所有 key 的映射都要重算，非常痛苦 😫
```

### ✅ 有 Virtual Shard 的情况：

```
textCopyEditkey → 哈希 → virtual_shard_123
virtual_shard_123 → 节点A
```

如果节点A挂掉或者系统扩容，只需要更改：

```
mathematica


CopyEdit
virtual_shard_123 → 节点C ✅
```

👉 原来的 key → virtual_shard 映射**不变**，其他数据不受影响！

------

## 🌐 类比 DNS（域名系统）

你的类比再次非常精确！

- 用户记住的是 `example.com`，**不是 IP 地址**
- 如果服务器 IP 改变了，只要 DNS 修改记录就行了，**不影响你的使用**

同样道理：

- 系统记住的是 `key → virtual shard` 映射
- 后端物理结构变化，只需更新 virtual shard → 实体节点 的映射即可

------

## ✅ 使用 Virtual Shard 的优势总结

| 优点                 | 描述                                             |
| -------------------- | ------------------------------------------------ |
| 🔗 **多一层间接映射** | 类似指针或 DNS，解耦了 key 和物理节点            |
| ⚖️ **更好负载均衡**   | virtual shard 数量可以远大于物理节点，分布更均匀 |
| ♻️ **更容易扩容缩容** | 添加/删除节点时只需重新映射一部分 virtual shard  |
| 🔄 **最小化影响范围** | 系统变更时只影响少数分片，不是所有数据           |
| 🧠 **简化逻辑**       | 应用程序不需要关心底层存储位置的变动             |

------

## 🔍 最终结构图（示意）

```
textCopyEdit         [key]
           ↓
    [哈希函数] → virtual_shard_57
           ↓
   virtual_shard_57 → 节点3（真实存储位置，如磁盘或实例）
```









### **实际案例：电商订单系统（使用 DynamoDB 架构）**

**场景**：某电商平台日均处理 **1000 万笔订单**，要求订单数据可实时写入，且支持按订单ID或用户ID快速查询。以下详细说明其存储流程：

------

### **步骤1：订单数据分片（自动分片）**

#### **分片规则**：

- **分区键（Partition Key）**：`订单ID`（例如 `ORD-2023-123456`）

- **分片计算**：对 `订单ID` 进行哈希运算，映射到 **固定数量的虚拟分片（Virtual Shard）**，每个虚拟分片对应一个物理存储节点。

  python

  复制

  ```
  # 示例哈希计算（假设哈希空间为 0~9999）
  shard_id = hash("ORD-2023-123456") % 10000  # 假设结果为 shard_id=5732
  ```

#### **分片动态调整**：

- 当分片数据量超过 **10GB** 时，系统自动将其分裂为两个子分片（如 `shard_5732` → `shard_5732_A` 和 `shard_5732_B`）。
- 新订单根据哈希结果自动路由到新分片。

------

### **步骤2：订单写入流程（LSM 树）**

#### **写入时序**：

1. **用户下单**：用户点击“支付”按钮，生成订单 `ORD-2023-123456`。

2. **路由到分片**：根据 `订单ID` 的哈希值，确定写入目标为分片 `shard_5732`。

3. **写入 WAL 日志**：

   - 将订单数据（JSON格式）追加写入分片 `shard_5732` 的 **Write-Ahead Log（WAL）** 文件。
   - **目的**：确保即使系统崩溃，数据不会丢失。

4. **写入 MemTable**：

   - 订单数据被插入到分片 `shard_5732` 内存中的 **MemTable**（有序跳表结构，按订单ID排序）。

   - **内存结构示例**：

     plaintext

     复制

     ```
     MemTable of shard_5732:
     ┌──────────────┬──────────────┐
     │ 订单ID       │ 数据指针      │
     ├──────────────┼──────────────┤
     │ ORD-2023-0001│ 内存地址0x123 │
     │ ORD-2023-0002│ 内存地址0x456 │
     │ ...          │ ...          │
     └──────────────┴──────────────┘
     ```

5. **返回成功**：用户立即看到“支付成功”页面。

------

### **步骤3：数据持久化（Compaction）**

#### **触发条件**：

- 当 `shard_5732` 的 MemTable 大小达到 **1GB**（可配置阈值），系统将其冻结为 **Immutable MemTable**，并创建新的 MemTable 接收写入。

#### **生成 SSTable**：

1. 后台线程将 `Immutable MemTable` 中的数据按订单ID排序，生成 **L0 层 SSTable 文件**，写入磁盘。
   - **文件示例**：`shard_5732_L0_001.sst`
2. 当 L0 层的 SSTable 数量超过阈值（如 4 个），触发 **Compaction**：
   - 合并多个 L0 SSTable，生成有序的 **L1 层 SSTable**（更大且无重复数据）。
   - **文件示例**：`shard_5732_L1_002.sst`

#### **合并优化**：

- **Leveled Compaction**：确保每层 SSTable 的 Key 范围不重叠，减少读取时扫描的文件数量。

------

### **步骤4：订单查询流程**

#### **按订单ID查询**：

1. 用户查询订单 `ORD-2023-123456`。
2. 计算 `shard_id = hash("ORD-2023-123456") % 10000 = 5732`，定位到分片 `shard_5732`。
3. **查询路径**：
   - **内存层**：先检查 `shard_5732` 的 MemTable。
   - **磁盘层**：若未命中，依次扫描 L0 → L1 → ... → Ln 层的 SSTable 文件。
   - **Bloom Filter**：每个 SSTable 附带布隆过滤器，快速判断订单是否存在。
4. **返回结果**：合并所有找到的数据版本（如多次更新同一订单），返回最新结果。

#### **按用户ID查询（使用全局二级索引 GSI）**：

1. 用户查询 `user_id=9527` 的所有订单。
2. DynamoDB 自动通过 **GSI**（以 `user_id` 为分区键的独立索引表）定位相关订单ID。
3. 再按订单ID回主表查询完整数据。

------

### **步骤5：自动扩展与故障处理**

#### **吞吐量扩展**：

- **预配置模式**：为 `shard_5732` 分配 1000 写入单位（WCU），突发流量时自动超卖至 3000 WCU。
- **按需模式**：完全由 DynamoDB 自动调整吞吐量，无需人工干预。

#### **分片故障恢复**：

- **多副本机制**：每个分片有 3 个副本（跨不同可用区）。
- 若主分片 `shard_5732` 宕机，10 秒内切换至副本分片，并重建新副本。

------

### **关键流程图解**

plaintext

复制

```
                [用户下单]
                    │
                    ▼
              [计算分片ID]
                    │
                    ▼
         ┌───────[写入WAL]───────┐
         │                      │
         ▼                      ▼
   [写入MemTable]         [复制到其他副本]
         │
[MemTable满]→[冻结为Immutable]→[生成SSTable]
         │                      │
         ▼                      ▼
    [新MemTable]          [Compaction合并]
                              │
                              ▼
                   [多层SSTable存储]
```

------

### **性能指标示例**

| 操作                 | 延迟     | 吞吐量         |
| :------------------- | :------- | :------------- |
| 订单写入（MemTable） | 1~5 ms   | 10万 TPS/分片  |
| 订单查询（内存命中） | 1~2 ms   | 5万 QPS/分片   |
| 订单查询（磁盘命中） | 5~10 ms  | 1万 QPS/分片   |
| Compaction           | 后台异步 | 不影响前台性能 |

------

### **总结**

通过 **LSM 树 + 自动分片**，该电商系统实现了：

- **高吞吐写入**：MemTable 和 WAL 保障快速响应。
- **自动扩展**：分片分裂和合并应对数据增长。
- **快速查询**：Bloom Filter 和索引优化减少 I/O。
- **高可用性**：多副本和自动故障转移。

这种架构完美支撑了“双11”等流量洪峰场景，同时保持运维成本最低。