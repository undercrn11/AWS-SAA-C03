# 手动水平搬送预约接口文档

**版本**: v1.0
 **更新日期**: 2024-01-15
 **接口路径**: `/wcs/task/appointment/manual/horizontal`
 **请求方式**: `POST`

------

## 1. 接口说明

本接口用于在手动模式下创建水平搬送任务的预约。系统支持批量指定取货库位和放货库位，实现货物的水平转运。

### 1.1 业务场景

- 手动指定AGV进行货物搬运
- 批量处理多个库位间的货物转移
- 特殊的TYT中转库位管理

### 1.2 接口特点

- 支持批量操作
- 事务性保证（全部成功或全部失败）
- 严格的前置条件检查
- 完整的库位状态验证

------

## 2. 请求说明

### 2.1 请求格式

http

```http
POST /wcs/task/appointment/manual/horizontal
Content-Type: application/json
```

### 2.2 请求参数

```json
{
    "RequiredAGVs": ["AGV001", "AGV002"],
    "Out": ["TYT-1-1-1", "A-2-3-4"],
    "In": ["B-3-4-5", "C-4-5-6"]
}
```

### 2.3 参数详细说明

```
参数名类型必填说明示例值
RequiredAGVsstring[]是指定执行任务的AGV编号列表["AGV001", "AGV002"]
Outstring[]是取货库位编号列表["TYT-1-1-1", "A-2-3-4"]
Instring[]是放货库位编号列表["B-3-4-5", "C-4-5-6"]
```

> ⚠️ **注意**: 参数名称大小写敏感，首字母必须大写

### 2.4 参数验证规则

#### 2.4.1 基础验证

1. 非空验证
   - 所有数组参数不能为 null 或空数组
   - 数组元素不能为空字符串
2. 长度匹配
   - `Out` 数组长度必须等于 `In` 数组长度
   - 表示一一对应的取放货关系

#### 2.4.2 业务规则验证

1. TYT库位要求
   - `Out` 或 `In` 中至少有一个库位必须以 "TYT" 开头
   - 不区分大小写（TYT、tyt、Tyt 均可）
2. 库位唯一性
   - `Out` 和 `In` 数组中不能有重复的库位
   - 同一库位不能既是取货位又是放货位

------

## 3. 响应说明

### 3.1 成功响应

**HTTP状态码**: `200 OK`

json

```json
{
    "success": true,
    "message": "Manual appointment created successfully"
}
```

### 3.2 错误响应

#### 3.2.1 参数验证失败（400）

```
错误场景响应消息
数组为空"Input, Out and In arrays cannot be null or empty."
长度不匹配"Out and In arrays must have equal length."
缺少TYT库位"At least one park in Out or In must start with 'TYT'."
库位重复"Duplicate parks found between Out and In: {库位号}"
```

#### 3.2.2 业务逻辑错误（400）

**1. 前置任务未完成**

```json
{
    "success": false,
    "message": "Previous TYT task (CommandId: xxx) is not completed. Status: 0, EndDateTime: null",
    "previousCommandId": "UI-Manual-Horizontal_20240101120000_abcd1234"
}
```

**2. 库位验证失败**

```json
{
    "success": false,
    "message": "错误详情"
}
```

可能的错误详情：

- `"Park {库位号} does not exist"` - 库位不存在
- `"Park {库位号} group must be 'Product', current: {当前值}"` - 库位分组错误
- `"Park {库位号} type must be one of [0,1,2,3], current: {当前值}"` - 库位类型错误
- `"Park {库位号} is unusable (unusable=1)"` - 库位不可用
- `"Park {库位号} is already appointed (appointment=1)"` - 库位已被预约

#### 3.2.3 服务器错误（500）

```json
{
    "success": false,
    "message": "Internal server error"
}
```

------

