关于应用层

Socket ：套接字（socket）是一个抽象层，应用程序可以通过它发送或接收数据，可对其进行像对文件一样的打开、读写和关闭等操作。套接字允许应用程序将I/O插入到网络中，并与网络中的其他应用程序进行通信。网络套接字是IP地址与端口的组合。

————————————————

Socket是一种编程接口，用于在应用程序中实现网络通信

**Socket TCP/IP 应用层关系**：

TCP/IP协议是网络通信的底层协议，提供了通信的基本规则。
Socket是一种编程接口，在计算机网络中，其主要用处是使应用程序能够使用TCP/IP协议或其他协议进行网络通信。
应用层协议是构建在Socket和TCP/IP之上的，用于实现不同应用程序之间的通信规则。它们利用Socket接口与传输层通信，最终使用TCP/IP协议在网络上传输数据。
————————————————

应用程序比如浏览器、电子邮件、文件传输服务器等产生的数据，会通过传输层协议进行传输。而应用程序是不会和传输层直接建立联系的，而是有一个能够连接应用层和传输层之间的套件，这个套件就是 Socket。

一个TCP socket包含以下五个部分：

1.协议（TCP）

2.本地ip地址：发送端口的ip地址

3.外部地址：目标端口的ip地址

4.状态：该socket目前的状态（LISTENING， ESTABLISHED，TIME_WAIT）

5.PID：用作socket的唯一标识符

cmd命令：netstat -ano,可用于查看本主机上的所有socket.

UDP socket 里没有目的ip地址和端口，所以在使用UDP协议传输数据时，需要向运输层发送报文，socket和目标地址信息。而使用TCP socket的话，只需传输报文和socket

TCP和UDP都是明文传输，没有加密。

所以TCP采用SSL(Secure Socket Layer)或TSL（Transport Layer Security），用于实现TCP的安全传输。

不同的应用对允许发送数据丢失的容忍度是不同的。有些应用不允许数据丢失或只能容忍很小一点的数据丢失。但有些应用能容忍一定程度的数据丢失。如多媒体应用。视频播放，语音聊天等。

除了对数据丢失敏感，还有对带宽敏感和对时间敏感两种特性。如PVP游戏，对事件比较铭感。视频会议对带宽敏感。

吞吐量：一条网络路径上，发送进程能够向接受进程交付比特的速率。

tracert命令用于查从你的电脑主机所在网域到目标网域需要通过哪里的路由。

所谓的AI学习是一个复杂函数的拟合过程。你通过不断的训练，让AI生成的函数符合当前的解决问题所需的函数。

关于客户端向服务器端请求资源，通常有两种方式：流水线方式和非流水线方式。

非流水线方式是指我发送一个请求，等请求的资源到我这里之后，再做出下一个请求。如此类推。

而流水线方式是我发送一个请求后，不等收到资源，立即又发送下一个请求，以此类推。此种方式适合请求在同一服务器端的资源。

向服务端的请求报文时		ASCII编码的,所以是可读的。其有头部(hander)和主体(body)组成.

使用tcp会造成粘包的问题,因为TCP向应用程序提供的服务是无边界的，所以意味着有可能TCP会将多个请求的数据合成同一个报文发送给请求的客户端。

粘包是指，短时间内向服务端发送多个请求，而服务端只返回了一个报文，客户端请求的数据被合并进了同一个报文。

所谓粘包问题主要还是因为:
1、接收方不知道消息之间的界限，不知道一个消息要提取多少字节的数据所造成的。 (服务器端出现黏包)
2、tcp在发送数据少且间隔时间短的数据时，会将几条和并一起发送。(客户端出现黏包)

tcp是数据流协议。

目前比较合理的处理方法是:为字节流加上一个报头,告诉发送的字节流总大小，然后接收端来一个死循环接收完所有数据。用struck将序列化后的数据长度打包成4个字节(4个字节完全够用)。
使用struct模块可以用于将Python的值根据格式符，转换为C语言的结构(byte类型)，便于数据流传输。

————————————————

目前解决粘包问题需要通过应用层自身的努力。

解决的具体方法有使用HTTP,HTTPS,网络socket,或FTP,SMTP协议。

HTTP1.0使用的是无状态连接，意思是其会创建一个TCP连接，当用户收到请求的回应后，连接就会被关闭。不管用户之后会不会再发出请求。

而	从HTTP1.1开始，HTTP开始支持持续连接，其可以支持在同一个TCP连接发送多个请求，和做出多次回复。其添加了一个Connection: keep-alive的属性在报文的header里面。用于告诉服务器端保持连接。

而从HTTP2开始，其支持在一个TCP连接中同时发送多个请求或回复报文。另外，Http2对报文的header进行了压缩，以减少传输的数据大小。

对于TCP三次握手中第三次可不可以携带数据。一般数据的传输需要在TCP连接建立后才开始。但TCP没有严格规定不能在握手期间向服务端传输数据，所以是有操作空间的。但目前没有这么做的先例。因为有可能会导致一系列的兼容性问题。

关于网络流量强度的问题，为什么流量强度接近1时会使排队时延增加，甚至会导致丢包。

流量强度是单位时间内，到达路由器的数据数量除以单位时间内路由器可以传输出去的数据数量。如果强度接近1，那证明，该链路绝大部分时间都在应对网络数据传输。任何多一点数据量的到来都有可能导致该链路无法应付。在网络上可能会造成用户需要排队等待。

而对付此类情况，通常的方法是在路由器上设置一定缓存用于储存用户请求的数据，当用户向服务器请求数据时，先检查缓存内是否有需要的数据，如果有则直接返回，没有才向服务器请求。

使用代理服务器也是同样的解决方法。

FTP有两种工作方式，一个为主动模式(PORT)，一个为被动模式(PASV).

主动模式是FTP协议最初定义的连接模式。

在开始一个FTP连接时，客户端程序首先会为自己随机分配一个TCP端口，它使用这个端口向服务器的FTP端口（默认21）发出连接请求，服务器接受请求后会建立一条控制链路，然后客户程序向服务器发出Port命令，告诉服务器它的数据通道的端口打开了。当需要传送数据时，服务器端口通过服务器自己的20端口向客户端提供的随机端口发送连接请求，请求被接受之后便开始传输数据，数据链路通道也就打开了， 在整个过程中，由于服务器在建立数据连接时主动发起连接，因此称为主动模式。

主动模式的问题是由于客户端提供的端口是随机的，如果客户端处于防火墙内部，由于防火墙大多会限制自问主机连接内部非开放端口，数据连接很可能被防火墙限制，造成无法建立数据连接，从而导致FTP无法正常工作。



被动模式：其连接过程是，在开始一个FTP连接时，客户端程序首先为自己随机分配一个TCP端口，它使用这个端口向服务器的FTP端口（默认21）发出连接请求，服务器接受请求后会建立一条控制链路，然后客户端程序发出一Pasv命令，要求服务器采用PASV模式建立数据连接，服务器便为自己随机分配一个数据通道端口，并将这个端口号告诉客户端程序。传送数据时，客户端程序采用另一个随机端口向服务器提供的数据通道端口发送连接请求，请求被接受之后便开始传输数据，数据链路通道也就打开了。在这个方式中，由于服务器端在建立数据连接时，被动接受客户端的连接请求，因此称为被动方式


注：被动方式，由于两个连接都由客户端发起，对于防火墙来说，一般不限制这样的访问，因此解决了防火墙内无法进行FTP的问题。

主动模式下，服务器会使用两个端口。一个是控制用的端口21，一个是用于数据传输的端口20。

而在被动模式下，服务器一样使用21端口用于控制连接。但用于传输的端口则是随机确定的，其会使用任意一个大于1023的端口。用于传输数据。

FTP和HTTP不同，其是一个有状态的协议，会通过session维护用户状态。所以不用担心当一分数据传输完后，服务器端会立即切断与客户端的连接。

但是有一个问题，FTP是一个连接建立一个专属的传输通道。所以，如果你要传输,比如说，21个文件，那么FTP会为你每一个文件都建立一个单独的通信渠道用于传输。所以会有21个单独的传输通道。当数据传输完成时，用于数据传输的通道链接会被删除，但用于控制的连接会被保留。所以传输大量文件时，会耗费大量资源用于创建和维护控制和传输数据的连接。

而且，FTP是铭文传输，无论是数据还是用于用户验证的控制信息等等。所以很容易被他人获取。所以现在都使用更先进，安全，高效的通信协议。如FTPS（FTP Secure）,或者SFTP(SSH file Transfer Protocol).前者是在FTP的基础上增加了SSL/TLS加密，后者是运行在Secure Shell Protocol上的FTP，而且可以在单一连接通道内传输多个数据文件。还有HTTP2，HTTP3也可达成更安全，多文件同时传输的要求。

FTP的用户代理是你使用的FTP软件的客户端。SMTP的用户代理是你使用的Email软件的客户端（如Gmail,qq邮箱等）。这些用户代理是用户和服务器之间的桥梁。它代替用户和服务器打交道。

SMTP的工作流程。用户编写完邮件后，点击发送。客户端会准备好发送邮件所需的东西（邮件本体，用于用户验证的信息凭证和其他所需的东西）。准备好后，客户端会从用户设定中获取于其连接的SMTP服务器的地址。然后客户端会和服务器建立TCP连接，通常连接到服务器的465端口（使用SSL/TSL的话）或587端口(使用STARTTLS的话)。然后客户端会发送echo命令，向服务器提示其支持ESMTP(Extended SMTP)

Client: EHLO client.example.com
Server: 250-smtp.example.com Hello client.example.com

然后双方会建立加密通信。然后会使用凭证进行用户验证。其具体行为是：客户端向服务器发送auth login的命令，同时其会向服务器发送用base64加密的用户信息，可以是用户名和密码，或者是使用token.

用户名密码：

Client: AUTH LOGIN
Server: 334 VXNlcm5hbWU6
Client: dXNlckBleGFtcGxlLmNvbQ==
Server: 334 UGFzc3dvcmQ6
Client: cGFzc3dvcmQ=
Server: 235 Authentication successful

token:

Client: AUTH XOAUTH2 dXNlckBleGFtcGxlLmNvbQ== base64token
Server: 235 2.7.0 Authentication successful

验证成功后，客户端会向服务器发送邮件，同时指明发送者的邮件地址和发送目的地的邮箱地址。

发送者：

Client: MAIL FROM:<user@example.com>
Server: 250 OK

目的地：

Client: RCPT TO:<recipient@example.com>
Server: 250 OK

邮件数据头部：

Client: DATA
Server: 354 Start mail input; end with <CRLF>.<CRLF>

邮件内容（主体）：

Client: Subject: Test Email
        This is a test email.
        .
Server: 250 OK: queued as 12345

完成以上动作后，服务器会分析目标地址所用的SMTP服务器地址，然后将所有信息发送到目的地邮箱的SMTP服务器。

目的地的用户打开他的邮箱客户端，其客户端会使用POP3（邮局访问协议）或IMAP（Internet邮件访问协议）或网页邮件界面访问邮件。

Gmail,Outlook,Yahoo Mail使用了HTTPS用于获取邮件。

原始的SMTP只能传输ASCII码的字符，所以在面对中文汉字和日语字符会无所适从。

traceroute <destination IP or domain>

此命令可用于在cmd查看目标ip需要从你这里（你所在的网络区域）经历多少跳才能到达目标ip.
