#### 有关AWS S3的个人理解与笔记



AWS S3（simple StorageService）是 AWS 提供的一种 **可扩展、高可用、持久性强** 的对象存储服务。它可以存储和检索任意数量的数据，并且可以通过互联网随时访问。

AWS S3 常见的应用场景包括：

- 存储和分发大型文件（图片、视频、备份等）
- 大数据存储（数据湖）
- 静态网站托管
- 云端应用存储
- 备份和灾难恢复

### **AWS S3 的主要特性**

1. **可扩展性**
   - S3 **自动扩展**，可以处理 **PB 级数据** 和 **每秒百万级请求**。
2. **高耐久性和高可用性**
   - AWS S3 提供 **99.999999999%（11 个 9）耐久性**，通过跨多个可用区（AZ）存储多个副本来保障数据安全。
   - 可用性取决于存储类别（如 **S3 标准级别 99.99% 可用性，S3 智能分层 99.9%**）。
3. **存储类别** AWS S3 提供多种存储类别，以满足不同的 **成本与性能需求**：
   - **S3 标准（Standard）** – 高耐久性、高可用性、高性能。
   - **S3 智能分层（Intelligent-Tiering）** – 自动在不同访问频率的存储级别之间移动数据。
   - **S3 标准-低频访问（Standard-IA）** – 适合不常访问的数据，存储成本较低，但访问费用较高。
   - **S3 单区-低频访问（One Zone-IA）** – 仅存储在一个可用区，价格更低，但无 AZ 级别的冗余。
   - **S3 Glacier & Glacier Deep Archive** – 超低成本的长期存档存储。
4. **安全性和访问控制**
   - **IAM 策略** – 通过 AWS 身份与访问管理（IAM）控制用户访问权限。
   - **存储桶策略（Bucket Policy）** – 允许设置 **S3 Bucket 级别的访问控制**。
   - **访问控制列表（ACLs）** – 控制单个对象的访问权限。
   - **加密支持** – 提供 **服务器端加密（SSE）** 和 **客户端加密**。
   - **VPC 端点（VPC Endpoints）** – 可在私有网络（VPC）中访问 S3，而无需通过公共互联网。
5. **数据管理与生命周期策略**
   - **版本控制（Versioning）** – 可以保留对象的多个版本。
   - **生命周期规则（Lifecycle Policies）** – 自动转换存储类别或在设定时间后删除数据。
6. **事件通知**
   - S3 可以在对象上传、删除或修改时触发 **AWS Lambda、SNS、SQS** 等服务。
7. **静态网站托管**
   - S3 可以 **直接托管静态网站**，支持 HTML、CSS 和 JavaScript。

------

### **AWS S3 计费模式**

AWS S3 的费用主要取决于：

- **存储类别**（标准、低频访问、Glacier 等）
- **数据传输**（传入免费，传出需付费）
- **请求类型**（PUT、GET、LIST 操作）
- **跨区域复制（Replication）** 成本

------

### **基本 AWS S3 操作**

使用 AWS CLI 进行 S3 操作的示例命令：

1. **创建存储桶（Bucket）**：

   ```
   bash
   
   
   CopyEdit
   aws s3 mb s3://my-bucket-name
   ```

2. **上传文件到 S3**：

   ```
   bash
   
   
   CopyEdit
   aws s3 cp myfile.txt s3://my-bucket-name/
   ```

3. **下载文件**：

   ```
   bash
   
   
   CopyEdit
   aws s3 cp s3://my-bucket-name/myfile.txt .
   ```

4. **列出存储桶中的对象**：

   ```
   bash
   
   
   CopyEdit
   aws s3 ls s3://my-bucket-name/
   ```

5. **删除对象**：

   ```
   bash
   
   
   CopyEdit
   aws s3 rm s3://my-bucket-name/myfile.txt
   ```

6. **同步本地文件夹到 S3**：

   ```
   bash
   
   
   CopyEdit
   aws s3 sync ./local-folder s3://my-bucket-name/
   ```

------

### **常见应用场景**

- **数据备份和归档**：日志存储、应用备份、灾难恢复。
- **大数据分析**：与 AWS Athena、Redshift、Glue 集成的数据湖。
- **Web 和移动应用**：存储用户上传的图片、视频、文档等。
- **静态网站托管**：结合 CloudFront 进行内容分发（CDN）。

------

### **AWS S3 与其他 AWS 存储服务的比较**

| 特性         | S3（对象存储）     | EBS（弹性块存储）  | EFS（弹性文件系统） |
| ------------ | ------------------ | ------------------ | ------------------- |
| **存储类型** | 对象存储           | 块存储             | 文件存储            |
| **适用场景** | 备份、存档、数据湖 | 操作系统盘、数据库 | 共享文件存储        |
| **可扩展性** | 自动扩展           | 固定大小           | 自动扩展            |
| **计费方式** | 按存储量、请求计费 | 按预配置容量计费   | 按使用量计费        |



##### S3有的设置（一部分）



###  **版本控制（Versioning）**

- **作用：**

  - 启用后，S3 **保存文件的多个版本**，防止意外覆盖或删除。
  - 上传同名文件时，**不会直接替换旧文件**，而是创建新的版本。

- **示例（上传同名文件后自动保留旧版本）：**

  ```
  bash
  
  
  CopyEdit
  aws s3 cp file.txt s3://my-bucket/
  ```

- **是否建议启用？** ✅ **是**，如果你需要**数据备份或恢复**。
   ❌ **否**，如果你**不想额外占用存储空间**。

------

### **2️⃣ 服务器端加密（Server-side Encryption）**

- **作用：**

  - 在数据存入 S3 时，**自动加密**，提高安全性。

- **加密方式：**

  1. **SSE-S3**（默认，AWS 负责密钥管理）
  2. **SSE-KMS**（使用 AWS KMS 密钥管理）
  3. **SSE-C**（使用你自己的密钥）

- **示例（启用 SSE-S3 加密）：**

  ```
  bash
  
  
  CopyEdit
  aws s3 cp file.txt s3://my-bucket/ --sse AES256
  ```

- **是否建议启用？** ✅ **是**，如果你**存储敏感数据**。
   ❌ **否**，如果你的数据**无需额外保护**。

------

### **3️⃣ 对象锁定（Object Lock）**

- **作用：**
  - 让文件**无法被删除或修改**，类似**WORM（Write Once, Read Many）**存储模式。
  - 常用于**合规性存储**（如金融、医疗数据）。
- **是否建议启用？** ✅ **是**，如果你**需要数据合规保护**。
   ❌ **否**，如果你**无需锁定文件**。

------

### **4️⃣ 生命周期规则（Lifecycle Rules）**

- **作用：**

  - **自动管理文件的存储周期**，将旧数据转移到**低成本存储层**，或在特定时间后**删除**。

- **常见策略：**

  - **30 天后转移至 Infrequent Access（低频访问存储层）**
  - **90 天后转移至 Glacier（归档存储）**
  - **365 天后删除旧版本**

- **示例（90 天后转移到 Glacier 归档存储）：**

  ```
  jsonCopyEdit{
    "Rules": [
      {
        "ID": "MoveToGlacier",
        "Filter": { "Prefix": "" },
        "Status": "Enabled",
        "Transitions": [
          { "Days": 90, "StorageClass": "GLACIER" }
        ]
      }
    ]
  }
  ```

- **是否建议启用？** ✅ **是**，如果你**希望降低存储成本**。
   ❌ **否**，如果你**需要长期访问所有数据**。

------

## **📌 S3 智能分层归档（Intelligent-Tiering Archive Configurations）**

- **作用：**

  - **自动管理数据的存储层级**，根据**访问频率**将文件移动到**更低成本的存储层**。

- **智能分层存储类别：**

  | **存储层**                                | **访问频率** | **读取时间** | **适用场景**       |
  | ----------------------------------------- | ------------ | ------------ | ------------------ |
  | **Frequent Access（频繁访问层）**         | 经常访问     | 立即         | 活跃数据           |
  | **Infrequent Access（低频访问层）**       | 30 天未访问  | 立即         | 备份、偶尔访问数据 |
  | **Archive Access（归档访问层）**          | 90 天未访问  | 12 小时      | 长期存储           |
  | **Deep Archive Access（深度归档访问层）** | 180 天未访问 | 48 小时      | 冷存储、合规存档   |

- **示例（启用智能分层归档）：**

  ```
  bashCopyEditaws s3api put-bucket-intelligent-tiering-configuration --bucket my-bucket --id myConfig --intelligent-tiering-configuration '{
      "Status": "Enabled",
      "Tierings": [
          { "Days": 90, "AccessTier": "ARCHIVE_ACCESS" },
          { "Days": 180, "AccessTier": "DEEP_ARCHIVE_ACCESS" }
      ]
  }'
  ```

- **是否建议启用？** ✅ **是**，如果你**存储大量非活跃数据**，希望降低成本。
   ❌ **否**，如果你的数据**需要长期高频访问**。

------

## **📌 Amazon EventBridge**

- **作用：**

  - **实时监测 AWS 事件**（如 S3 文件上传、EC2 服务器启动）并**触发自动操作**。
  - 适用于**自动化流程、事件驱动架构**。

- **常见应用场景：**

  | **使用场景**       | **示例**                              |
  | ------------------ | ------------------------------------- |
  | **S3 事件处理**    | 当文件上传到 S3 时触发 Lambda 函数    |
  | **AWS 服务监控**   | 当 EC2 服务器异常停止时发送警报       |
  | **自动化安全监控** | 当安全组被修改时通知管理员            |
  | **数据管道触发**   | 当新数据到达 S3 时，启动数据 ETL 任务 |
  | **与第三方集成**   | 将 AWS 事件发送到 Slack 或自定义 API  |

- **示例（S3 文件上传时触发 Lambda 函数）：**

  ```
  bashCopyEditaws events put-rule --name "S3UploadRule" --event-pattern '{
      "source": ["aws.s3"],
      "detail-type": ["Object Created"],
      "detail": {
          "bucket": {
              "name": ["my-bucket"]
          }
      }
  }' --state ENABLED
  ```

  **绑定 Lambda 函数**

  ```
  bashCopyEditaws events put-targets --rule "S3UploadRule" --targets '[{
      "Id": "1",
      "Arn": "arn:aws:lambda:us-east-1:123456789012:function:ProcessS3Upload"
  }]'
  ```

- **是否建议启用？** ✅ **是**，如果你需要**自动化事件处理**（如 S3 上传触发 Lambda）。
   ❌ **否**，如果你的 S3 **只是普通存储**，不需要自动化。

------





在S3中上传了文件后，可以通过URL访问该文件。但S3 bucket的默认设置是拒绝所有外界访问。所以只有你自己，即此个文件系统的所有者可以访问此系统内的文件。然后，在URL中会有一个临时的证书来证明访问的人是你。所以你可以访问到系统内的文件。如果你开启了公共访问，那么，网络上的所有人都可以随意访问该文件系统下的任意文件。而URL中将不会产生临时证书。



在AWS S3中，主要有两种方式控制bucket里的资源可以被谁访问。

第一种是通过User-Based Policies (也就是IAM Policies)

IAM policies的例子

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": ["s3:GetObject"],
      "Resource": "arn:aws:s3:::my-bucket/*",
      "Principal": { "AWS": "arn:aws:iam::123456789012:user/Alice" }
    }
  ]
}

或者制作一个一个IAM role，授予这个role 对应的权限，然后把它给EC2 instance。以让EC2 instance获得对应的权限。

第二种是使用Resource-Based Access(bucket Policies)

这种和IAM policies有些类似。其也是用json格式的policies来控制。不过，其控制的是谁可以访问或不能访问某些资源。

编写例子：

{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Principal": "  *  " ,
      "Action": "s3:GetObject",
      "Resource": "arn:aws:s3:::my-bucket/public-images/*   ”"
    }
  ]
}

在S3中，可以使用生成器来生成bucket policies

##### 有关S3 的Static Website Hosting

在S3上，你可以托管一个静态网站。其支持语言为HTML，css，javascript。

你需要制作一个html文件，并把它上传到S3上面。HTML上路径可以设置为S3上的文件，这样通过网站，其他人也可以看到S3 bucket的内容。当然，此前提是你要把S3 bucket设置为可公共访问。S3不支持databaase，有关服务器端的数据处理，不支持PHP，node.js,java,python等语言，不支持动态内容生成（如workpress,Django, Laravel等）。但是，还是有办法在S3上托管动态网站。

需要以下的AWS服务一起合作才行：

| **Feature Needed**      | **Solution**                        |
| ----------------------- | ----------------------------------- |
| **User Authentication** | AWS Cognito                         |
| **Database / Backend**  | AWS Lambda + API Gateway + DynamoDB |
| **Forms Submission**    | API Gateway + Lambda + DynamoDB     |
| **Real-time Updates**   | WebSockets (API Gateway)            |
| **Process Data**        | AWS Lambda                          |







### **如何在 S3 开启版本控制后真正删除文件？**

S3 **版本控制（Versioning）**的工作方式如下：

✅ **S3 会为每个文件分配一个唯一的版本 ID**。
 ✅ **当你上传新版本文件时，旧文件不会被覆盖，而是隐藏**，但仍然存在。
 ✅ **删除文件时，S3 只会添加一个“删除标记”（Delete Marker）**，并不会真正删除文件。
 ✅ **你可以通过删除“删除标记”来恢复文件**，但文件的所有版本仍然存在。
 ✅ **要真正删除文件，你必须手动删除它的所有版本**。

------

## **🔹 如何在 S3 版本控制启用的情况下永久删除文件？**

由于 **禁用版本控制不会删除旧版本文件**，你必须按以下步骤操作：

### **✅ 1. 查找文件的所有版本**

运行以下命令，**列出该文件的所有版本**：

```
bash


CopyEdit
aws s3api list-object-versions --bucket my-bucket --prefix my-file.txt
```

这将显示：

- **删除标记（如果文件已被删除）**
- **所有旧版本及其版本 ID**

------

### **✅ 2. 删除删除标记（如果存在）**

如果文件已被**删除**，它会有一个 **Delete Marker（删除标记）**，隐藏了旧版本。

**删除删除标记的命令：**

```
bash


CopyEdit
aws s3api delete-object --bucket my-bucket --key my-file.txt --version-id DELETE_MARKER_ID
```

➡️ 这样 **文件将恢复可见，但旧版本仍然存在**。

------

### **✅ 3. 彻底删除所有文件版本**

要**真正删除文件**，你必须**逐个删除所有版本**。

**删除指定版本的命令：**

```
bash


CopyEdit
aws s3api delete-object --bucket my-bucket --key my-file.txt --version-id VERSION_ID
```

➡️ 你需要对**每个版本 ID**都执行该命令，确保完全删除。

------

### **✅ 4.（可选）使用生命周期规则自动删除旧版本**

如果你不想手动删除，可以使用 **S3 生命周期规则（Lifecycle Rules）** 自动清理旧版本。

**示例：自动删除 30 天前的旧版本**

```
jsonCopyEdit{
  "Rules": [
    {
      "ID": "DeleteOldVersions",
      "Status": "Enabled",
      "NoncurrentVersionExpiration": {
        "NoncurrentDays": 30
      }
    }
  ]
}
```

➡️ 这样，**所有 30 天前的旧版本都会自动删除**，无需手动操作。

------

## **🔹 禁用版本控制能删除文件吗？**

❌ **不，禁用版本控制不会删除已存在的旧版本**。

- **如果你禁用版本控制，S3 只是不再创建新版本**。
- **所有已存储的旧版本仍然存在，不会自动删除**。
- 你仍然需要 **手动删除旧版本** 或 **使用生命周期规则自动清理**。

------

## **📌 总结：如何真正删除 S3 版本化的文件？**

| **操作**             | **结果**                                |
| -------------------- | --------------------------------------- |
| **上传新版本**       | 旧文件不会被覆盖，但会被隐藏。          |
| **删除文件**         | S3 只会添加“删除标记”，旧版本仍然存在。 |
| **删除“删除标记”**   | 文件恢复可见，但旧版本依然存在。        |
| **删除特定版本**     | 该版本永久删除。                        |
| **禁用版本控制**     | 仅停止创建新版本，不影响旧版本。        |
| **使用生命周期规则** | 自动删除过期的旧版本，减少存储成本。    |

请注意，如果不开启verioning,则在S3上删除某个文件的操作是永久生效的。在此情况下，bucket不会有delete marker，删除了，文件就没了，如果选择更新，旧的文件不会被保留。





#### S3 replication

S3也有replication，但只能从元bucket到目的地bucket。而且因为versioning,S3的replication 和RDS的replicas和EBS的snapshot和AMI都不同。S3的replication有两种。Cross-Region Replication (CRR)和Same-Region Replication (SRR)。另外需要注意的一点是S3的replication是没有链式效应的。也就是说，如果你想把元bucket复制到其他两个或以上的replicaiton bucket。只能手动一个一个完成。比如：A->B,A->C,A->D。




##### 以下是S3 的replication的特别之处：



### **在启用版本控制的存储桶和未启用版本控制的存储桶之间可以进行复制吗？**

**简短答案：**
 ❌ **不可以，S3 复制（Replication）要求源和目标存储桶都启用版本控制（Versioning）。**

------

## **🔹 为什么不能在启用了版本控制的存储桶和未启用版本控制的存储桶之间进行复制？**

1. **S3 复制必须依赖版本控制**
   - S3 **复制需要版本控制**来跟踪哪些对象已经被复制。
   - **未启用版本控制的存储桶无法追踪对象的不同版本**，导致复制无法进行。
2. **源存储桶和目标存储桶必须同时启用版本控制**
   - 如果你尝试从**启用了版本控制的存储桶**复制到**未启用版本控制的存储桶**，AWS 会报错。

### **✅ 解决方案：如何在两者之间复制数据？**

如果你想要在 **版本控制存储桶** 和 **非版本控制存储桶** 之间移动文件，你可以使用：

- **S3 批量操作（Batch Operations）**（适用于大批量数据复制）
- **AWS Lambda + S3 事件**（用于自动化复制）
- **S3 `sync` 命令**（用于手动复制）

**示例：手动将已启用版本控制的存储桶同步到未启用版本控制的存储桶**

```
bash


CopyEdit
aws s3 sync s3://source-versioned-bucket s3://destination-non-versioned-bucket
```

➡ **此命令仅复制最新版本的文件，不会保留历史版本。**

------

## **🔹 删除标记（Delete Marker）在复制时会发生什么？**

如果 **源存储桶中包含删除标记（Delete Marker）**，其行为如下：

| **场景**                                 | **默认行为**                 | **启用“删除标记复制”后？**   |
| ---------------------------------------- | ---------------------------- | ---------------------------- |
| **源存储桶中存在删除标记**               | ❌ 默认不会复制到目标存储桶   | ✅ 删除标记会被复制           |
| **对象被删除（源存储桶中只剩删除标记）** | ✅ 目标存储桶中的对象仍然存在 | ✅ 目标存储桶的对象也会被删除 |

### **✅ 如何复制删除标记**

默认情况下，S3 **不会复制删除标记**。
 如果你希望删除标记也被复制：

1. **进入 S3 控制台 → 选择存储桶 → “管理（Management）” → 复制规则（Replication Rules）**
2. **编辑复制规则，启用删除标记复制（Enable delete marker replication）**

------

## **📌 总结**

| **场景**                                               | **行为**                                       |
| ------------------------------------------------------ | ---------------------------------------------- |
| **从启用版本控制的存储桶复制到未启用版本控制的存储桶** | ❌ 不支持                                       |
| **从未启用版本控制的存储桶复制到启用版本控制的存储桶** | ❌ 不支持                                       |
| **源存储桶中存在删除标记**                             | ❌ 默认不会复制到目标存储桶                     |
| **启用删除标记复制**                                   | ✅ 删除标记会被复制，目标存储桶中的对象会被删除 |

需要注意一点：只有你启用了replication后，上传，或修改的文件才会被复制到目的地bucket，而在启用replication之前就上传的文件，是不会被同步到目的地bucket的。而需要强调的一点是，当启用了delete marker replication后，你在元bucket对某个文件执行删除操作后，目的地bucket只会复制其文件的delete marker.不包含任何文件，只有一个标记。



#### S3 storage class

|                      | S3 Standard                                                  | S3 Intelligent-Tiering*                            | S3 Express One Zone**                | S3 Standard-IA                   | S3 One Zone-IA**             | S3 Glacier Instant Retrieval       | S3 Glacier Flexible Retrieval***   | S3 Glacier Deep Archive***       |
| :------------------: | :----------------------------------------------------------- | :------------------------------------------------- | :----------------------------------- | :------------------------------- | :--------------------------- | :--------------------------------- | :--------------------------------- | :------------------------------- |
|       使用案例       | 用于对经常访问的数据进行通用存储                             | 适用于访问模式未知或不断变化的数据，可自动成本节省 | 高性能存储，可存储您最频繁访问的数据 | 需要毫秒级访问的不经常访问的数据 | 可重新创建的不经常访问的数据 | 每年通过即时检索访问几次的长期数据 | 备份和存档很少访问且成本低廉的数据 | 存档非常少访问且成本非常低的数据 |
|      首字节延迟      | 毫秒                                                         | 毫秒                                               | 几毫秒                               | 毫秒                             | 毫秒                         | 毫秒                               | 分钟或小时                         | 小时                             |
|        持久性        | Amazon S3 提供最持久的云端存储。基于其独特的架构，S3 的数据持久性设计为超过 **99.999999999%（11 个 9）**。此外，默认情况下，S3 至少跨 3 个可用区冗余存储数据，其内置弹性可应对大范围灾难。客户可以将数据存储在单个可用分区中以最大限度地降低存储成本或延迟，也可以将数据存储在多个可用分区中以防止整个数据中心永久丢失，或者将数据存储在多个 AWS 区域中以满足地理弹性要求。 |                                                    |                                      |                                  |                              |                                    |                                    |                                  |
|   设计可实现可用性   | 99.99%                                                       | 99.9%                                              | 99.95%                               | 99.9%                            | 99.5%                        | 99.9%                              | 99.99%                             | 99.99%                           |
|      可用性 SLA      | 99.9%                                                        | 99%                                                | 99.9%                                | 99%                              | 99%                          | 99%                                | 99.9%                              | 99.9%                            |
|        可用区        | ≥3                                                           | ≥3                                                 | 1                                    | ≥3                               | 1                            | ≥3                                 | ≥3                                 | ≥3                               |
| 最低存储持续时间费用 | 不适用                                                       | 不适用                                             | 1 小时                               | 30 天                            | 30 天                        | 90 天                              | 90 天                              | 180 天                           |
|       检索费用       | 不适用                                                       | 不适用                                             | 不适用                               | 每检索 1GB                       | 每检索 1GB                   | 每检索 1GB                         | 每检索 1GB                         | 每检索 1GB                       |
|     生命周期转换     | 是                                                           | 是                                                 | 否                                   | 是                               | 是                           | 是                                 | 是                                 | 是                               |

\* S3 Intelligent-Tiering 收取小额监控和自动化费用，对自动分层有 128KB 的最小合格对象大小限制。更小的对象也可以存储，但将始终按频繁访问层费率收费，不收取监控和自动化费用。有关更多信息，请参阅 [Amazon S3 定价](https://aws.amazon.com/cn/s3/pricing/)。 存档访问层和深度存档访问层中的标准检索是免费的。如果您需要从存档访问层中更快地访问您的对象，您可以使用 S3 控制台为加快的检索付费。S3 智能分层频繁和不频繁访问层的首字节延迟为毫秒访问时间，存档访问和深度存档访问层的首字节延迟为分钟或小时。

** 当发生 AWS 可用区全部或部分丢失或损坏的极少数情况下，One Zone 存储类中的数据可能会丢失。例如，火灾和水灾等事件可能导致数据丢失。除了这些类型的事件外，我们的 One Zone 存储类使用与区域存储类相似的工程设计来保护对象免受独立磁盘、主机和机架级故障的影响，并且每种类型都被设计为提供 99.999999999% 的数据持久性。

S3 Glacier Flexible Retrieval 和 S3 Glacier Deep Archive 还需要额外为每个存储对象提供 40 KB 元数据，其中包括按 S3 Glacier Flexible Retrieval 费率收费的识别和检索数据所需的 32 KB 元数据。另外 8 KB 数据按 S3 Standard 费率收费，这是维护归档到 S3 Glacier Flexible Retrieval 的对象的自定义名称和元数据所必需的数据。





亚太地区（东京）



|                                        | PUT、COPY、POST 或 LIST 请求（每 1000 个请求） | GET、SELECT 和所有其他请求（每 1000 个请求） | 生命周期转换请求（每 1000 个请求） | 数据取回请求（每 1000 个请求） | 数据取回（每 GB） |
| :------------------------------------: | :--------------------------------------------: | :------------------------------------------: | :--------------------------------: | :----------------------------: | :---------------: |
|            **S3 Standard**             |                   USD 0.0047                   |                 USD 0.00037                  |               不适用               |             不适用             |      不适用       |
|      **S3 Intelligent-Tiering** *      |                   USD 0.0047                   |                 USD 0.00037                  |              USD 0.01              |             不适用             |      不适用       |
|                频繁访问                |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|               不频繁访问               |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|                归档即时                |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|             存档访问，标准             |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|             存档访问，批量             |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|             存档访问，加急             |                     不适用                     |                    不适用                    |               不适用               |           USD 11.00            |     USD 0.033     |
|           深度存档访问，标准           |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|           深度存档访问，批量           |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|  **S3 Standard-Infrequent Access** **  |                    USD 0.01                    |                  USD 0.001                   |              USD 0.01              |             不适用             |     USD 0.01      |
|      **S3 Express One Zone** ***       |                   USD 0.0024                   |                 USD 0.00019                  |               不适用               |             不适用             |     0 USD***      |
| **S3 Glacier Instant Retrieval** ****  |                    USD 0.02                    |                   USD 0.01                   |              USD 0.02              |             不适用             |     USD 0.03      |
| **S3 Glacier Flexible Retrieval** **** |                  USD 0.03426                   |                 USD 0.00037                  |            USD 0.03426             |           请参见下方           |    请参见下方     |
|                  加急                  |                     不适用                     |                    不适用                    |               不适用               |           USD 11.00            |     USD 0.033     |
|                  标准                  |                     不适用                     |                    不适用                    |               不适用               |           USD 0.0571           |     USD 0.011     |
|               批量 ****                |                     不适用                     |                    不适用                    |               不适用               |             不适用             |      不适用       |
|           预置容量单位 ****            |                     不适用                     |                    不适用                    |               不适用               |             不适用             | 每单位 USD 110.00 |
|    **S3 Glacier Deep Archive** ****    |                   USD 0.065                    |                 USD 0.00037                  |             USD 0.065              |           请参见下方           |    请参见下方     |
|                  标准                  |                     不适用                     |                    不适用                    |               不适用               |           USD 0.1142           |     USD 0.022     |
|                  批量                  |                     不适用                     |                    不适用                    |               不适用               |           USD 0.025            |     USD 0.005     |
|  **S3 One Zone-Infrequent Access** **  |                    USD 0.01                    |                  USD 0.001                   |              USD 0.01              |             不适用             |     USD 0.01      |

上述 S3 生命周期转换请求定价表示对该存储类的请求。

\* 在全部五个访问层：频繁、不频繁、归档即时、归档和深度归档访问层中，S3 Intelligent-Tiering 标准和批量数据取回和恢复请求都是免费的。在已恢复的对象上调用的后续恢复请求将按 GET 请求计费。加快取回可用于 S3 Intelligent-Tiering 归档访问层，并按照加快请求和取回费率计费。

** S3 Standard-IA 和 S3 One Zone-IA 存储将按最短存储时间 30 天计费。未满最短存储时间就被删除、覆盖或转移到其他存储类的对象将产生正常存储使用费，以及针对最短存储时间的剩余天数按比例收取的费用。存储时间超过最短存储时间的对象不会产生最少费用。

***S3 Express One Zone 对最大 512 KB 的请求按请求收取固定费用。对于请求中超过 512KB 的部分，按 GB 额外收取 PUT 和 GET 请求的费用。在 AWS 区域欧洲地区（爱尔兰）、亚太地区（孟买）、美国东部（弗吉尼亚州北部）、美国东部（俄亥俄州）、美国西部（俄勒冈州）和欧洲地区（斯德哥尔摩），PUT 的费用为 0.008 USD/GB，GET 的费用为 0.0015 USD/GB；在 AWS 区域亚太地区（东京），PUT 的费用为 0.0075 USD/GB，GET 的费用为 0.0014 USD/GB。

*** 被归档到 S3 Glacier Instant Retrieval 和 S3 Glacier Flexible Retrieval 的对象将按 90 天最短存储时间收费，而 S3 Glacier Deep Archive 的最短存储时间为 180 天。在最短存储时间前就被删除的对象所产生的按比例支付的费用等于按剩余天数支付的存储费用。未满最短存储时间就被删除、覆盖或转移到其他存储类的对象将产生正常存储使用费，以及针对最短存储时间的剩余天数按比例收取的费用。存储时间超过最短存储时间的对象不会产生最少费用。S3 Glacier Flexible Retrieval 批量数据取回和请求是免费的。



#### 关于S3 replication的RTC

### **什么是 S3 复制中的 RTC（Replication Time Control，复制时间控制）？**

**RTC（复制时间控制）\**是 Amazon S3 \*\*复制设置中的一个可选功能\*\*，它提供\**15 分钟内的复制保证**。

🚀 **标准 S3 复制**是**异步的**，意味着**无法保证数据复制的速度**——可能需要几分钟到几小时。

✅ **RTC 确保 99.99% 的对象在 15 分钟内复制完成**，适用于**关键、对时间敏感的应用场景**。

------

## **🔹 RTC 的作用**

1. **加快复制速度** → 确保 **99.99% 的对象在 15 分钟内复制完成**。
2. **提供复制监控** → 通过 **Amazon CloudWatch 指标** 监控复制性能。
3. **适用于合规性和灾难恢复** → 确保数据几乎实时备份。
4. **支持同区域复制（SRR）和跨区域复制（CRR）** → 适用于 **SRR（Same-Region Replication，同区域复制）** 和 **CRR（Cross-Region Replication，跨区域复制）**。

------

## **🔹 RTC 与标准 S3 复制的区别**

| **功能**     | **标准 S3 复制**                     | **RTC（复制时间控制）**                |
| ------------ | ------------------------------------ | -------------------------------------- |
| **复制速度** | 无保证时间（可能需要几分钟到几小时） | **99.99% 的对象在 15 分钟内复制完成**  |
| **监控**     | 无实时监控                           | **提供 CloudWatch 指标，监控复制时间** |
| **适用场景** | 普通复制（低优先级）                 | **关键任务、对时间敏感的数据**         |
| **成本**     | 标准费用                             | **按复制的数据量额外收费**             |

------

## **🔹 如何在 S3 复制中启用 RTC**

### ✅ **通过 AWS 控制台启用**

1. **进入 S3 控制台 → 选择你的存储桶**。
2. **点击“管理（Management）” → 复制规则（Replication Rules） → 创建规则**。
3. **在 "复制时间控制 (RTC)" 选项下，启用 RTC**。
4. **设置目标存储桶和权限**。
5. **保存复制规则**。

------

### ✅ **通过 AWS CLI 启用 RTC**

在设置复制规则时启用 RTC：

```
jsonCopyEdit{
  "Role": "arn:aws:iam::123456789012:role/s3-replication-role",
  "Rules": [
    {
      "Status": "Enabled",
      "Priority": 1,
      "DeleteMarkerReplication": { "Status": "Enabled" },
      "Filter": { "Prefix": "" },
      "Destination": {
        "Bucket": "arn:aws:s3:::destination-bucket",
        "ReplicationTime": {
          "Status": "Enabled",
          "Time": { "Minutes": 15 }
        },
        "StorageClass": "STANDARD"
      }
    }
  ]
}
```

然后运行：

```
bash


CopyEdit
aws s3api put-bucket-replication --bucket my-bucket --replication-configuration file://replication.json
```

------

## **🔹 什么时候应该使用 RTC？**

| **应用场景**                         | **是否应该使用 RTC？** |
| ------------------------------------ | ---------------------- |
| **用于灾难恢复的实时备份**           | ✅ 是                   |
| **多区域活跃数据同步**               | ✅ 是                   |
| **合规性和法规要求（金融、医疗等）** | ✅ 是                   |
| **大规模数据复制（但对时间不敏感）** | ❌ 否                   |
| **需要降低成本的应用场景**           | ❌ 否                   |



#### 关于S3 Event Nofications

S3可以设立一系列的通知，来告诉你某个，或多个S3 bucket里发生了什么。

S3可以和SNS，SQS，Lambda function一起使用以达到通知的目的。但S3需要IAM Permissions。因为，本质上，是S3向AWS内的其他组件发起通讯。

和其一起合作的通常为AWS Eventbridge。Eventbridge为一个中介服务，也像数据库里的触发器。



## EventBridge 的角色：谁在监听？谁在处理？

| 角色                                | 是谁                             | 作用                                                         |
| ----------------------------------- | -------------------------------- | ------------------------------------------------------------ |
| 🧼 **事件生产者（发送者）**          | AWS 服务、自定义应用或 SaaS 应用 | **发送事件** 到 EventBridge，例如 EC2 状态变更、Lambda 函数发出的事件，或你自己写的后端系统。 |
| 👂 **EventBridge（监听者和路由器）** | EventBridge 事件总线             | **监听事件**，根据规则进行匹配，然后**路由**事件到目标。     |
| 🛠 **事件消费者 / 目标（处理者）**   | Lambda、Step Functions、SQS 等   | **处理事件**——运行逻辑、更新数据库、发送通知等等。           |

------

### 🔁 事件流简要流程：

1. **生产者** 产生一个事件（例如用户注册，或 S3 上传文件）。
2. **EventBridge** 接收该事件并放入事件总线。
3. EventBridge 使用 **事件规则** 来检查是否匹配。
4. 如果匹配，EventBridge 将事件发送给一个或多个 **目标**。
5. **目标** 接收到事件并进行处理（例如 Lambda 调用图像处理逻辑）。

------

## 🔧 示例场景：

### 场景：

> 每当上传一张图片到 S3，就触发一个 Lambda 函数进行处理。

- **事件生产者**：S3（自动发送 "PutObject" 类型事件）
- **事件监听者（路由器）**：EventBridge（默认总线）
- **事件规则**：匹配 `detail-type` 为 `"Object Created"` 且 bucket 名为 `"my-photo-bucket"` 的事件
- **事件处理者（目标）**：Lambda 函数（处理图像）

所以在这个场景中：

- S3 发送事件 ➡️
- EventBridge 监听并匹配 ➡️
- Lambda 接收到事件并处理它。

------

## 🔁 可以有多个目标吗？

可以！对于一个事件，你可以定义多个目标，例如：

- Lambda：执行业务逻辑
- SQS：记录日志
- SNS：发送通知

这些目标都通过规则来“监听”事件，而 EventBridge 是中心的 **监听者和路由器**。

要让 **EventBridge** 正常工作，需要对整个流程的 **三个部分**都进行一些设置或配置。下面是每一部分需要做的事情：

------

## ✅ 需要设置的三大部分：

### 1. **事件发送者（Producer）**

需要让某个服务或程序 **发送事件** 到 EventBridge。

- **AWS 服务**（比如 S3、EC2、DynamoDB 等）会自动发事件到默认总线（Default Event Bus）——你不需要额外配置发送。

- 如果是 

  你自己的程序

  ，你需要使用 AWS SDK 或 CLI 来调用：

  ```
  bash
  
  
  CopyEdit
  aws events put-events --entries [...]
  ```

  或者在代码中用 

  ```
  PutEvents
  ```

   API（支持 Python、Node.js、Java 等语言）

------

### 2. **EventBridge（监听器 / 路由器）**

你需要在 EventBridge 中进行如下设置：

- **创建规则（Rule）**：告诉 EventBridge 要监听哪类事件（根据事件的 JSON 结构筛选）
- **绑定目标（Target）**：当事件匹配规则后，要把它发送给谁（Lambda、SQS、Step Functions 等）

📝 举个例子：

```
jsonCopyEdit事件规则：
{
  "source": ["aws.s3"],
  "detail-type": ["Object Created"],
  "detail": {
    "bucket": {
      "name": ["my-bucket"]
    }
  }
}
```

------

### 3. **事件处理者（目标服务 / Dealer）**

你还需要设置最终处理事件的地方，比如：

- **Lambda 函数**：要先创建好函数，并设置权限让 EventBridge 可以调用它。
- **SQS 队列**：需要配置队列、权限、消费者。
- **Step Functions、ECS、SNS 等**：都需要先配置好目标服务。

------

## 🔐 额外注意：权限设置（IAM）

你还需要注意权限：

- **事件发送者** 要有权限调用 `events:PutEvents`
- **EventBridge** 要有权限调用目标服务（比如 Lambda）
- 可以用 **IAM 角色** 或 **资源策略** 来授予这些权限

------

## 🧭 总结一下配置流程：

| 步骤 | 配置对象    | 你要做的事情                                   |
| ---- | ----------- | ---------------------------------------------- |
| 1️⃣    | 事件发送者  | 配置 AWS 服务或自定义程序来发送事件            |
| 2️⃣    | EventBridge | 创建事件规则 + 设置事件目标                    |
| 3️⃣    | 事件处理者  | 配置处理事件的服务（Lambda、SQS 等）并设置权限 |



#### S3 Pre-Signed URLs

用于你像给AWS用户之外的人某个文件的读写权限，但不想让bucket从私有变为公共状态。这种情况下，可以通过生成Pre-Signed URL做到这点。这个URL里有短暂的权限认证，能让外部人士访问到该文件。可以对此URL做过期时间设定，可以设定分钟或小时。



##### S3的Object lock

s3可以对某个object进行单独的锁定设置。这种设置是为了保护文件其本身，因时间的推移，而被删除掉。此功能使用了一个名为WORM（Write once Read Many）的模型。这种模型是一种数据存储模型。其规定一个文件，或数据，只能被写入一次，无法被修改或删除，可以被读无数次。其被经常用于审计文件储存，医院文件储存等。在S3的Object Lock中，可以对某单一的文件进行几种锁定设置：1.Governance Mode，文件被锁定，但有相应特殊权限的的角色（role）或者root可以进行修改，或删除此锁定设置。2.Compliance Mode，任何人都无法改变其锁定设置，知道此锁定规则过期。



#### S3 Object Lambda

假设你有大量用户资料存储在S3 bucket里，有用户需要访问这些资料，但其中的一部分信息不能被人知道。此时可以使用Object Lamdba。首先，创建一个access point，然后把这个access point接入AWS Lambda，然后在Lambda上编写过滤规则（程序或json policies），Lambda会把处理过后的数据转给用户。



#### S3 Access Point

对于某个S3 bucket，我们可以为其设置访问接入点。其作用为为访问该bucket的用户或外部访问者分流。如某个S3的bucket里，有多个文件夹，其目录下有多个文件。不同的用户需要访问bucket内的不同文件夹，而你只想让其访问此文件夹，可以使用access point，进行设置允许其访问该文件夹下的文件。当然，默认的设置路径是整个bucket，需要通过Policies来进行设置。

请注意，当access point没有被创立时，还可通过公共URL或固定的静态网址（需要设置）来访问某一个文件或bucket。前提是该bucket允许公共访问。或者通过IAM permission，IAM Role赋予AWS服务权限来访问S3 bucket。