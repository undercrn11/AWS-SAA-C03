关于计算机网络

··		TCP/IP和OSI模型
OSI模型（open system interconnection）是由国际标准化组织ISO在1984年提出的用于解决不同网络之间兼容性问题的计算机网络通信的基础模型。
OSI模型分为七层：
physical layer 物理层：负责在物理媒介上传输原始比特流，包括电缆、光纤等。
data link layer 数据链路层：负责将比特流组织成帧，进行物理寻址和错误检测。以太网协议和PPP协议、ARP协议，是数据链路层的典型实现。
network layer 网络层：提供数据包的传输服务，实现源到目的地的路径选择。IP协议是网络层最为重要的协议之一。
transport layer 传输层： 负责端到端的通信，确保数据的可靠传输。TCP和UDP是传输层最常见的协议，分别提供可靠的连接和无连接的通信服务。
session layer 	会话层：进行主机之间的通信。 建立、管理和终止会话，确保数据的同步和控制。RPC（远程过程调用）和NetBIOS是会话层的典型协议。
presentation layer表示层：负责数据的格式转换、加密和解密。ASCII和JPEG等编码和压缩算法属于表示层的范畴。
application layer 应用层：为用户提供网络服务，是用户与网络之间的接口。HTTP、FTP、SMTP等协议属于应用层。


物理层细节：1.将比特流转换为模拟信号或数字信号（电压、光信号等）2.不同的传输介质都有其特定的传输速率，距离限制。

物理层的例子：
1.以太网 物理层的电缆（如双绞线，同轴电缆）传输比特流
2.光纤通信 ’将比特流转换为光信号，通过光纤进行传输，然后再转换为接收端的比特流。
3.无线通信（蓝牙，wifi等）通过无线信道传输比特流。

primitive(原语)
原语是一种在计算机网络中被使用的动作或指令。它用于网络栈的不同层之间的通信。
原语主要有以下几种：
  1.请求（request）
  2.提示（indication）
  3.回复（response）
  4.确认   (confirm)
有关原语使用的例子：
1.Application Layer (like your email app) sends a request to the Transport Layer to send an email.
2.Transport Layer uses an indication to tell the Network Layer to prepare the data for sending.
3.Network Layer processes the request and sends a response back to the Transport Layer saying it's ready.
4.Once the email is sent, the Transport Layer sends a confirm message back to the Application Layer.

网络栈中，每一层的协议的实现需要借助下面的层所提供的服务才能实现，实现本层的协议的目的是为了向上层提供更好的服务。



现在的TCP/IP协议中看不到会话层和表示层是因为这两层的功能，或所提供的服务由现在的应用层的应用程序来完成。应用程序可以创造，使用自己独有的会话管理，数据加密，解密功能，标准以适应自身的需求。



数据送到目的主机后，目标主机把物理层的比特流收集起来，进行分析，判断出帧头，帧尾，把帧取出来，交给数据链路层，数据链路层把帧里的数据部分拿出来，形成分组。然后交给网络层。网络层把分组内的报文段拿出来，交给传输层。传输层把报文段中的信息（message）部分拿出来，交给应用层。应用层收到信息，把message交给对应的应用进程。

分布式应用或分布式系统：运行在多台与网络连接的电脑中的软件系统。分布的意思是：该软件的不同部分，分布在不同地点或不同主机中。

如：手机上的应用程序，其前端运行在用户的手机上，而后端，服务器则运行在其他地方的某个或多个主机上。

分布式系统的好处主要在于分布，模块化和节省性能：把系统的不同部分分开，利于维护和升级，因为系统之间的模块彼此独立，互不影响。而相比在同一主机上运行系统的所有模块，把其独立出来明显更能减轻计算机系统的负担。而且当某一应用被大量用户访问时，还可以利用后端系统分布的特性，将用户的连接分布到不同的服务器主机上，以减轻系统负担，加快响应速度。

网络边缘：

我们日常使用的计算机，手机，平板电脑等电子设备被称为网络边缘。通常，我们把与网络相连的的计算机和其他设备称为端系统。其原因是其处于网路的边缘。

在商用网络发展的初期，普遍使用电话线来接入网络，但问题是，普通的电话线接入网络后，会占用所有的带宽。所以，当接入网络后就无法使用电话语音功能。

当时普遍会使用猫，英语叫modem，调制解调器。其作用是将电脑上的数字信号转化成可以在电话线上传输的模拟信号。

模拟信号：指在 时域 上数学形式为 **连续函数** 的 信号。在通信中，使用电信号来模拟出其他信号。如图像信号，声波信号等。一般说模拟信号或数字信号时，不加说明的话，指的都是电信号。

信号是信息的载体，信息是信号的内容。一个信号是模拟还是数字信号取决于其如何变识别和读取。

举个例子：因为模拟信号的波形是连续，变化平滑的。所以用来表示数字的话可以表示的非常精细，如100.0002.但数字型号只有高位和地位。对应0和1.所以数字信号表示出来的波形是呈现阶梯状的。

模拟信号的抗干扰性是比较差的。为了解决这个问题，需要将模拟信号转化成数字信号。

因为数字信号波形只有两种，高和低，所以即使信号在传输过程中被干扰而导致失真，只要能区分高低，就能还原出原本的信号，而且如果信号传输距离特别长，可以在中途增加一个中继器，将信号还原回原本的样子后继续传输，所以数字信号的抗干扰能力是比较强的。

数字信号的值是离散的，它的任何一个值都是规定的最小数单位的整数倍。

后期的DSL(Digital Subscriber Line)数字用户线，也是使用电话线用于传输数据。也是用modem,但DSL的modem，和原来的modem不太一样。DSL是使用更高频段用于传输信息，而原有的用于电话的低频段则不再使用，所以使用DSL可以兼顾打电话和接入网络。

接入网络的物理媒介：

目前，接入网络主要通过以下两种类型的物理媒介。

第一种是引导型媒介。第二种是非引导型媒介。

引导型（导向型）媒介的特点是能将电磁波导向，沿着固体媒介传播。

此类媒介常见的有：双绞（铜）线，也就是以前常用的电话线。同轴电缆，常见于电视，机顶盒等。光纤。

非引导型（非导向型）媒介：在自由空间内，用各种介质（空气，真空，海水等）进行传播媒介。

此类媒介常见的有：无线电波，微波，红外线，激光。

微波通信频率较高，频段范围宽，所以数据率很高。通常被用作地面微波接力通信或卫星通信。

红外，激光通信在日常生活中最为常见的体现为遥控器。



赫兹：

用于描述波的频率。

如，某件事在一秒内发生1000次，则它的频率就为1000Hz。

带宽用于描述每秒可以传输多少数据（多少比特bit）

无论频分复用还是时分复用，要分，就要有多个单独的可分配资源。

以频分复用为例：一个基站会同时发射多个独立的，不同频率的信号频段，或多个基站，每个基站单独且同时发射一个不同频率的信号。

然后不同用户会被分配到不同的信号频段中。

然后需要传输的数据，会被转换成对应频率的信号，然后在双绞线，同轴电缆或光纤中传输。

对于c/s和p2p两种模式的理解：

c/s模式很好理解，你是客户端，你向服务器端发送请求，服务器端收到请求，按情况做出回应。然后将信息或数据传回给客户端。

此模式的好处在于，只需保证服务器可以处理请求和数据回传即可。即只需关注服务器端。但问题是，随着用户数量增长，服务器端的性能有可能无法支持如此多的用户同时访问。

p2p的特点是每个人都是服务器端，也是客户端。但p2p能正常使用的前提是，在你使用p2p的客户端时，需要有别人也在使用p2p的客户端。因为你需要向他人请求数据，如果没有其它人使用p2p客户端，或你的客户端可以连接到的其他人里，没有人有你需要的资源的话，你也无法得到你想要的东西。

而c/s模式只需要关注服务器和网络就行了.

P2P架构的应用也有客户端进程和服务器进程之分

连接路由器和wifi通常使用双绞线。

![68fff61efb41a2d41bce1ea4ffb6f433](C:\Users\msduser\Desktop\学习笔记\assets\68fff61efb41a2d41bce1ea4ffb6f433.jpg)

而如果你的设备是将调制解调器（猫）和WiFi结合在一起的话，只需使用光纤光缆将WiFi接入住宅的网络接入点便可（墙上的）。

![4decc10393ff9d3d8a0ee7a99e3ce1cc](C:\Users\msduser\Desktop\学习笔记\assets\4decc10393ff9d3d8a0ee7a99e3ce1cc.png)

*DSL*（Digital Subscriber Line）意为数字用户线路，是指以电话线为传输介质的传输技术组合。*DSL*包括A*DSL*(Asymmetric Digital Subscriber Line，非对称数字用户线)、RA*DSL*、H*DSL*和V*DSL*等等。

所以一般说电话线接入网，就是在说DSL。

而同轴电缆（Coxical Cable）也可用于接入网。

![09b173fb0864fed85acf06ec1c4c1c89](C:\Users\msduser\Desktop\学习笔记\assets\09b173fb0864fed85acf06ec1c4c1c89.jpg)



使用电话线和同轴电缆都需要使用调制解调器进行信号转换。而且两者使用的调制解调器并不为同一种类。



### DSL调制解调器与同轴电缆调制解调器的区别

#### DSL调制解调器

**连接类型**

- **介质:** 铜质电话线。
- **服务:** 数字用户线路（DSL）服务，包括ADSL、VDSL等。

**技术**

- DSL技术:

   DSL调制解调器使用各种DSL技术通过传统的铜质电话线传输数据。主要类型包括：

  - **ADSL（非对称DSL）:** 提供比上传速度更高的下载速度。
  - **VDSL（超高速DSL）:** 提供比ADSL更快的速度，在下载和上传速度上都有所提升。

- **调制技术:** 使用离散多音（DMT）调制技术将数据编码到电话线上。

**物理接口**

- **电话线连接:** 通过电话线（RJ11接口）连接到电话插座。
- **以太网端口:** 提供以太网端口（RJ45接口）以连接到路由器或直接连接到计算机。

**性能**

- 速度:

   DSL速度根据DSL类型和与服务提供商中心办公室的距离而有很大差异。

  - **ADSL:** 典型下载速度高达24 Mbps，上传速度高达3.5 Mbps。
  - **VDSL:** 速度可达100 Mbps下载和50 Mbps上传。

**覆盖范围和距离**

- **距离敏感:** DSL性能随与中心办公室的距离增加而降低；距离越远，速度越慢。
- **典型使用场景:** 常用于没有电缆或光纤连接的农村或郊区地区。

#### 同轴电缆调制解调器

**连接类型**

- **介质:** 同轴电缆（用于有线电视系统）。
- **服务:** 有线电视公司提供的有线互联网服务。

**技术**

- DOCSIS技术:

   同轴电缆调制解调器使用数据传输规范（DOCSIS）技术通过电缆网络传输数据。

  - **DOCSIS版本:** 从DOCSIS 1.0到最新的DOCSIS 3.1和即将推出的DOCSIS 4.0，每个版本都提供更高的速度和功能。

- **调制技术:** 使用正交振幅调制（QAM）将数据编码到电缆网络中。

**物理接口**

- **同轴电缆连接:** 通过同轴电缆插座（F型连接器）连接。
- **以太网端口:** 提供以太网端口（RJ45接口）以连接到路由器或直接连接到计算机。

**性能**

- 速度:

   电缆调制解调器的速度通常比DSL更高且更稳定。

  - **DOCSIS 3.0:** 支持高达1 Gbps的下载速度和200 Mbps的上传速度。
  - **DOCSIS 3.1:** 支持高达10 Gbps的下载速度和1-2 Gbps的上传速度。

- **共享带宽:** 带宽在邻近用户间共享，峰值使用时速度可能会降低。

**覆盖范围和距离**

- **距离敏感性较低:** 性能受距离影响较小，只要在电缆网络服务区内，速度保持一致。
- **典型使用场景:** 常用于拥有成熟电缆电视基础设施的城市和郊区地区。

### 关键区别总结

| 特征         | DSL调制解调器              | 同轴电缆调制解调器          |
| ------------ | -------------------------- | --------------------------- |
| 连接介质     | 铜质电话线                 | 同轴电缆                    |
| 技术         | DSL（ADSL，VDSL）          | DOCSIS（1.0至3.1及更高）    |
| 物理连接     | 电话线（RJ11接口）         | 同轴电缆插座（F型连接器）   |
| 典型速度     | 高达100 Mbps（VDSL）       | 高达10 Gbps（DOCSIS 3.1）   |
| 距离敏感性   | 随距离增加性能下降         | 在服务区内距离影响较小      |
| 共享带宽     | 否（每用户独立线路）       | 是（邻近用户共享带宽）      |
| 最佳使用场景 | 无电缆/光纤连接的农村/郊区 | 拥有电缆基础设施的城市/郊区 |

总结来说，虽然DSL和同轴电缆调制解调器都用于提供互联网连接，但它们在技术、性能、物理接口和所用基础设施方面有显著不同。电缆调制解调器通常提供更高的速度和更稳定的性能，尤其是使用现代DOCSIS标准，而DSL调制解调器通常在没有电缆或光纤连接的区域使用。



### 信号转换总结

- **DSL调制解调器:**
  - 将来自计算机的数字信号转换为可以在电话线上传输的高频信号。
  - 将传入的高频信号转换回计算机的数字信号。
- **同轴电缆调制解调器:**
  - 将来自计算机的数字信号转换为可以在同轴电缆网络上传输的射频信号。
  - 将传入的射频信号转换回计算机的数字信号。

虽然DSL和同轴电缆调制解调器都执行调制和解调，但它们主要在数字框架内工作。DSL调制解调器处理电话线上的高频信号，同轴电缆调制解调器处理同轴电缆上的射频信号。两者都不是简单地将数字信号转换为传统意义上的模拟信号，而是适应其各自物理介质的传输

### 对于两者在信号上的总结

- **电话线:** 电话线（铜线）可以传输模拟语音信号和高频的DSL数字数据。DSL信号虽然在物理上是模拟信号，但它们承载的是数字信息。
- **同轴电缆:** 同轴电缆可以传输有线电视信号和数字数据。通过DOCSIS标准传输的数据信号在物理上是模拟的射频信号，但它们承载的是数字信息。

混合光纤同轴电缆（HFC，Hybrid Fiber-Coaxial）是一种广泛使用的宽带网络架构，结合了光纤和同轴电缆的优势，以提供高带宽和可靠的互联网服务。以下是关于HFC的详细解释：

### HFC网络架构

**结构:**

- **光纤部分:** 从互联网服务提供商（ISP）的核心网络开始，光纤电缆将信号传输到靠近用户的本地节点。光纤能够传输高带宽的数字信号，覆盖长距离而信号损失最小。
- **同轴电缆部分:** 在本地节点之后，信号通过同轴电缆传输到最终用户（家庭或企业）。同轴电缆可以有效地在较短距离内传输数据，同时利用现有的有线电视基础设施。

### 信号传输

**光纤部分:**

- **数字信号传输:** 光纤电缆主要传输数字信号，利用光脉冲在玻璃或塑料纤维中传输数据。这种传输方式具有高带宽和低延迟的优点。

**同轴电缆部分:**

- **模拟和数字信号传输:** 同轴电缆部分使用射频（RF）信号传输数据，这些信号可以是模拟的，但承载的是数字信息。通过DOCSIS（数据传输服务接口规范）标准，这些信号可以在同轴电缆上传输互联网数据。

### HFC网络的优势

1. **高带宽:** HFC网络利用光纤的高带宽能力，能够提供高速互联网服务。
2. **可靠性:** 光纤部分覆盖长距离，减少了信号损失和干扰，提高了整体网络的可靠性。
3. **利用现有基础设施:** HFC网络利用现有的同轴电缆基础设施，降低了网络部署成本。

### HFC网络的工作原理

1. **信号生成:** 互联网服务提供商在其核心网络中生成数字信号，通过光纤电缆传输。
2. **光纤到节点:** 数字信号通过光纤传输到靠近用户的本地节点（光纤节点）。
3. **光电转换:** 在本地节点，光信号转换为射频信号，这些信号通过同轴电缆传输到最终用户。
4. **用户设备:** 用户的调制解调器接收射频信号，并将其解调为用户设备（如计算机、智能手机）可以使用的数字数据。

### 总结

**HFC网络（混合光纤同轴电缆）**结合了光纤和同轴电缆的优势，以提供高带宽和可靠的互联网服务。光纤部分传输高带宽的数字信号，覆盖长距离，而同轴电缆部分则利用现有的有线电视基础设施，在较短距离内传输数据。这种架构在提供高速互联网服务的同时，减少了网络部署成本，并提高了网络的可靠性。
