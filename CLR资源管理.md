# CLR资源管理

CLR（公共语言运行时）会自动管理**托管资源**。这些资源包括：

1. **托管对象的内存**：
   - 任何在.NET中使用`new`关键字创建的对象（例如`string`，`List<T>`，自定义类的实例等）都由CLR管理。
   - 垃圾回收器（GC）会自动跟踪这些对象，并在它们不再使用时回收内存。
2. **自动变量初始化**：
   - CLR确保所有变量自动初始化为默认值。例如，整数初始化为`0`，对象初始化为`null`等。
3. **对象的引用计数**：
   - CLR管理对象的引用。当对象不再有引用时，垃圾回收器会识别它并最终清理内存。
4. **对象生命周期管理**：
   - CLR确保仍有引用的对象（存活的对象）会保留在内存中，只有在没有引用时，垃圾回收器才会回收它们的内存。
5. **自动资源清理（对于托管资源）**：
   - 像数组、列表、字典、字符串等托管资源，当它们超出作用域且不再被引用时，垃圾回收器会自动清理。

### **不**由CLR自动管理的资源

CLR **不** 会自动管理**非托管资源**。这些资源需要显式管理：

1. **非托管内存**：
   - 在托管堆外部分配的内存，例如通过直接使用`Marshal`或与本机代码的互操作，CLR不会自动管理这些内存。
   - 开发者必须手动释放这部分内存。
2. **数据库连接**：
   - 数据库连接（例如`SqlConnection`，`OleDbConnection`）是非托管资源。尽管它们被封装在托管类中，但底层的网络连接或文件句柄必须显式关闭。
3. **文件句柄**：
   - 打开磁盘上文件的文件流（例如`FileStream`，`StreamReader`，`StreamWriter`），CLR不会自动关闭。必须显式关闭它们，或者使用`using`语句来确保正确释放。
4. **网络套接字**：
   - 网络资源，例如套接字（例如`Socket`，`TcpClient`，`UdpClient`），是非托管的，必须显式关闭以释放网络连接。
5. **图形句柄**：
   - 图形资源的句柄，例如位图、字体或设备上下文（例如`Bitmap`，`Graphics`），CLR不会管理，需要显式释放。
6. **与本机代码的互操作**：
   - 当使用平台调用服务（P/Invoke）或COM互操作调用本机代码时，任何由本机代码分配的资源必须显式释放。
7. **手动内存管理**：
   - 如果直接使用非托管内存（例如通过`Marshal.AllocHGlobal`），必须使用`Marshal.FreeHGlobal`手动释放它。
8. **定时器和事件处理程序**：
   - 某些托管对象，例如`Timer`或事件处理程序，如果没有正确释放或取消订阅，可能会导致内存泄漏，因为它们可能继续引用那些原本已经超出作用域的对象。

### 总结

- **托管资源**：标准.NET对象的内存（例如类、字符串、数组等）由CLR自动管理。垃圾回收器负责分配和释放这些内存。
- **非托管资源**：任何与外部世界交互的资源，如文件句柄、网络连接、数据库连接或与本机代码的互操作，都必须显式管理。这些资源需要正确的释放以避免资源泄漏。

在实际应用中，如果您处理上述任何非托管资源，必须实现`IDisposable`并使用`Dispose`方法或`using`语句，以确保在不再需要这些资源时正确释放它们。