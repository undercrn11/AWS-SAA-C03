### 关于AWS S3的加密功能的理解与笔记



**在AWS S3中，文件加密方式有服务端加密和客户端加密两种。**

**服务端加密（Server-Side Encryption）有：**

**1.SSE-S3：**

**加密启用下的默认加密方式，使用AES256作为加密算法。由S3完全托管，不用任何管理。**

**2.SSE-KMS（KMS-Managed Keys）：**

**使用AES256作为加密算法，但密钥的生成和管理由AWS KMS来管理**

**3.SSE-C（Customer）：**

**使用AES256作为加密算法，你自己上传自己生成的key，相比其他两种方式，自由度相对高一点。但只能在AWS CLI的情况下使用，如使用AWS Console，则加密页面中选项不会出现此方法。**

**4.DSSE_KMS（Dual-Layer Server-Side Encryption with AWS KMS-Managed Keys）：一种增强型的加密算法，**

**其加密两次，第一次和SSE-KMS相同，第二次使用S3自己管理的独立key进行加密，适合于对安全性比较高的场景（可以在Console或CLI中启用）。**

**CLI中所需的代码：**

**aws s3api put-bucket-encryption \**
  **--bucket my-secure-bucket \**
  **--server-side-encryption-configuration '{**
    **"Rules": [**
      **{**
        **"ApplyServerSideEncryptionByDefault": {**
          **"SSEAlgorithm": "aws:kms",**
          **"KMSMasterKeyID": "your-cmk-id"**
        **},**
        **"BucketKeyEnabled": true,**
        **"DoubleEncryptionEnabled": true**
      **}**
    **]**
  **}'**

**请注意，如如果使用非S3自己管理，而是AWS管理的key时，S3的加密和解密需要使用API来调用AWS内的其他服务（如KMS）、而且，AES256为对称算法，所以加密和解密共用一个key。**

**SSE—KMS的key详解：在KMS中会用到3种key，第一种是AES-256 key，其为AES算法中用到的的key。用于加密数据。第二种是AES-256 data key。其为短暂被使用的key，被用于加密某个特定的object或者数据。用完就没。第三种为CMK (Customer Master Key)。其为KMS中被长期使用的，用于加密或解密第二种key的key。**



### 对于SSE—KMS的加密详解：



#### 第 1 部分：SSE-KMS（使用 AWS KMS 的服务器端加密）是如何运作的？

### 💡 运作机制（简化流程）

当你将对象上传到 S3，并选择了 **SSE-KMS**（可以是显式选择，也可以通过bucket策略配置）：

1. **S3 向 AWS KMS 请求一个数据密钥**。
2. 这个密钥是一个 **AES-256 数据密钥**。
3. **KMS 不会返回你的主密钥（CMK）**，只会返回生成好的数据密钥。
4. KMS 返回两个密钥内容：
   - ✅ **明文数据密钥**（用于加密对象）
   - ✅ **已加密的数据密钥**（使用你的 CMK 加密后得到的）
5. S3 使用 **明文数据密钥和 AES-256 加密算法**对对象进行加密。
6. 然后，S3 会存储以下内容：
   - 🔐 **加密后的对象**
   - 🔐 **加密后的数据密钥**（保存在对象的元数据中）

------

### 🛡️ KMS 如何保护主密钥（CMK）

- **CMK 永远不会被暴露**。
- 它存储在 **AWS KMS 中，托管于通过 FIPS 140-2 第 3 级认证的硬件安全模块（HSM）**。
- 你可以定义：
  - 谁可以使用该密钥（通过 IAM 策略或 KMS 密钥策略）
  - 允许哪些操作（如加密、解密等）
- 如果启用，AWS 还支持每年**自动轮换 CMK 密钥**。

------

### 🔍 审计与日志

- 所有涉及 CMK 的操作（如密钥使用、解密请求）都会被记录在 **AWS CloudTrail** 中。
- 你可以追踪：
  - 是谁访问了密钥、
  - 在什么时间、
  - 从什么位置、
  - 以及执行了什么操作。

------

## 🔐 第 2 部分：DSSE-KMS（双层加密的服务器端加密 with AWS KMS）

**DSSE-KMS = SSE-KMS + 一层额外的加密保护**

------

### 🔄 DSSE-KMS 加密流程：

1. **S3 从 KMS 获取一个数据密钥**（与 SSE-KMS 相同）。
2. 使用该 **AES-256 数据密钥对对象进行加密**。
3. 然后，S3 再应用一个 **第二层加密**：
   - 使用另一个由 **S3 独立管理的密钥**（不通过 KMS）。
   - 使用 **AES-256 算法再加密一次**。
4. 对象现在被**使用两个互不相关的密钥进行了双重加密**。

------

### 🔐 DSSE-KMS 中的密钥存储与隔离

| 加密层  | 密钥类型         | 管理方                | 存储位置           |
| ------- | ---------------- | --------------------- | ------------------ |
| 第 1 层 | AES-256 数据密钥 | KMS 生成并用 CMK 加密 | 存储在对象元数据中 |
| 第 2 层 | AES-256 密钥     | 由 S3 独立管理        | 用户无法访问或查看 |

------

这使得 **DSSE-KMS 更加安全**，特别适合以下场景：

- 你需要防范 **内部威胁**；
- 你有合规或监管需求，需要使用 **多层加密机制**。

------

## 🧠 KMS 密钥管理：你可以控制什么？

使用 KMS 的 CMK（客户管理密钥），你可以：

- 创建自定义策略的 **客户管理密钥（CMK）**；
- 启用 **自动密钥轮换（每年）**；
- 使用 **别名** 管理密钥，提升组织性；
- 创建 **跨区域密钥**，提高可用性；
- 通过 **KMS 密钥策略 + IAM**，为密钥设置 **精细化权限控制**。





## 什么是 **信封加密（Envelope Encryption）**？

> **信封加密** 是一种安全设计模式，指的是：
>  **使用数据密钥对数据进行加密**，然后再用一个更高级别的主密钥对这个数据密钥进行加密（即“包裹”加密）。

------

### 🎯 为什么使用这种方式？

- ✅ **性能更高**：对称加密（如 AES）速度快，适合大文件。
- ✅ **可扩展性好**：每个对象可以有自己专属的数据密钥，而主密钥可以统一管理。
- ✅ **安全性强**：主密钥极少暴露，降低被攻击风险。

------

## 🧱 关键组成部分

| 组件                    | 说明                                                         |
| ----------------------- | ------------------------------------------------------------ |
| **数据加密密钥（DEK）** | 一个随机生成的对称密钥（如 AES-256），用于加密实际数据。     |
| **主密钥（CMK）**       | 一个长期保存的密钥，仅用于加密/解密数据密钥。在 AWS 中由 KMS 托管。 |
| **加密后的数据密钥**    | 将数据密钥通过主密钥加密后的结果，用于安全存储。             |

------

## 🔐 信封加密的详细流程

### 🧩 **加密过程**

1. ✅ 生成一个 **数据加密密钥（DEK）**
   - 例如：一个随机的 256 位 AES 密钥。
2. ✅ 使用这个数据密钥对数据进行加密。
3. ✅ 使用主密钥（CMK）对数据密钥进行加密（包裹）。
4. ✅ 存储：
   - 加密后的数据
   - 加密后的数据密钥（一起保存）

------

### 🔓 **解密过程**

1. ✅ 读取加密数据和加密的数据密钥。
2. ✅ 使用主密钥（通过 KMS）解密出数据密钥。
3. ✅ 使用明文数据密钥解密原始数据。

------

## 🔐 AWS S3 中的实际案例：SSE-KMS

### 当你上传对象时：

1. S3 向 KMS 请求一个数据密钥。
2. KMS：
   - 生成一个新的 AES-256 数据密钥
   - 返回：
     - ✅ 明文数据密钥（S3 用它加密对象）
     - ✅ 已加密的数据密钥（使用 CMK 加密后）
3. S3：
   - 使用明文数据密钥加密文件
   - 存储加密后的文件 + 加密后的数据密钥（保存在对象元数据中）

### 当你下载对象时：

1. S3 获取加密的数据密钥。
2. 发送到 KMS 解密，KMS 使用 CMK 解密出明文数据密钥。
3. S3 用明文密钥解密对象并返回。

------

## 🧠 信封加密示意图

```
plaintextCopyEdit[你的数据] 
   |
[使用 AES-256 数据密钥加密]
   |
[加密后的数据]
   |
[数据密钥] -----> [使用 CMK 加密]
                          |
                [加密后的数据密钥] 
```

最终存储内容：

- 加密数据
- 加密后的数据密钥

------

## 🧩 信封加密的优势

| 特性               | 优势                                                      |
| ------------------ | --------------------------------------------------------- |
| 🔒 **更高安全性**   | CMK 永不离开 KMS；数据密钥是一次性使用的短期密钥          |
| 🚀 **更好性能**     | 对称加密适合处理大数据量                                  |
| 🔄 **支持密钥轮换** | 轮换 CMK 时只需重新加密数据密钥，而不需要重新加密所有数据 |
| 📦 **可扩展性强**   | 每个对象可以有自己的密钥，适合海量对象                    |
| 📜 **可审计性好**   | 所有密钥使用记录都可通过 CloudTrail 审计追踪              |

------

## 🧰 使用信封加密的 AWS 服务

- ✅ Amazon S3（使用 SSE-KMS 或 DSSE-KMS）
- ✅ AWS Lambda 的环境变量加密
- ✅ Amazon RDS 的透明数据加密（TDE）
- ✅ DynamoDB / EBS 的卷加密
- ✅ 自定义应用程序的安全加密实现





## 如果我不使用 SSE-S3，是否就必须自己调用 API 来加密或解密数据？

### ✅ 如果你使用 **SSE-KMS**：

是的，**S3 会在后台自动调用 KMS API** 来完成：

- 每次上传对象时：生成数据密钥
- 每次下载对象时：解密数据密钥

> 但注意：**你不需要自己写代码去调用 KMS**，这些操作由 S3 自动完成。

你只需要在上传时指定 SSE-KMS（或通过存储桶默认设置），S3 就会帮你完成所有加密逻辑和 KMS 通信。

------

### ⚠️ 如果你使用的是 **客户端加密（Client-Side Encryption）** 或自己管理加密：

那么确实需要你**自己调用加密和解密 API**，可能是你自己的服务，也可能是 AWS KMS。这样会涉及更多开发工作。

------

## 🔹每个文件都调用 KMS，不会太多吗？

这个问题非常专业！确实，KMS 有访问限制，但 AWS 是为大规模使用场景设计的。

### 📊 AWS KMS 默认吞吐量限制（每个区域）：

| 指标                       | 默认值（可以提升）                         |
| -------------------------- | ------------------------------------------ |
| **Encrypt / Decrypt 调用** | 每秒约 1000–5500 次                        |
| **GenerateDataKey 调用**   | 每秒约 1000–5500 次                        |
| 可否提升？                 | ✅ 可以，提交工单申请，甚至可提升至数万 TPS |

> 🧠 注意：KMS 只负责加密“数据密钥”，不是加密整个文件。数据密钥仅为 256 位，加解密速度非常快。

对于需要 **超高吞吐量（如每分钟 100 万次）** 的系统，AWS 推荐：

- ✅ **启用 Bucket Key（桶级缓存密钥）**
  - `BucketKeyEnabled=true`
  - 它会在存储桶级别缓存数据密钥，大幅减少 KMS 调用次数
- ✅ 使用 **SSE-S3**，如果你不需要严格的密钥管理
- ✅ 自己实现信封加密，只在极少数场景中使用 KMS 解密数据密钥，减少调用频率

------

## 🔐 安全性方面：KMS 返回的明文密钥会被黑客截获吗？

### ✅ 不会，原因如下：

- 明文数据密钥**只存在于 S3 后端的内存中**
- 加密后的数据密钥保存在对象的元数据中，供解密时使用
- **S3 和 KMS 的通信是通过加密的内网通道（TLS + IAM 授权）进行**
- 客户端（你）**永远拿不到明文数据密钥**
- 下载文件时，**S3 会在服务端完成解密**，然后通过 HTTPS 把解密后的文件返回给你

> 🔐 简单来说：**密钥不会通过网络发送出去，只有加密数据或解密后的数据通过加密连接返回给你**

------

## 📌 总结

| 问题                      | 答案                                                     |
| ------------------------- | -------------------------------------------------------- |
| 我需要自己调用 KMS 吗？   | ❌ 不需要，S3 会自动调用                                  |
| KMS 的性能够用吗？        | ✅ 是的，而且可以开启桶密钥或使用 SSE-S3 来提升性能       |
| 密钥会被截获吗？          | ❌ 不会，明文密钥从不离开 AWS 后台系统                    |
| 会不会遇到 KMS 调用限制？ | ⚠️ 可能，但可以申请提升配额；开启桶密钥能有效减少调用次数 |

------

## ✅ 建议使用方案

| 场景                                    | 建议加密方式                                |
| --------------------------------------- | ------------------------------------------- |
| **高并发 IOPS（如日志分析、视频上传）** | 使用 **SSE-S3** 或 **SSE-KMS + Bucket Key** |
| **合规要求高（如金融、医疗、政府）**    | 使用 **SSE-KMS** 或 **DSSE-KMS**            |
| **应用层高度敏感数据**                  | 使用 **客户端加密（CSE）**，自己管理密钥    |





## 什么是 FIPS 140-2 Level 3 认证的 HSM？

这是由三部分关键要素组成的：

- **FIPS 140-2** → 美国政府制定的加密模块安全标准
- **Level 3（第三级）** → 该标准中的高级安全等级之一
- **HSM（硬件安全模块）** → 一种专门用于保护加密密钥的高安全级别硬件设备

------

### 1️⃣ 什么是 FIPS 140-2？

**FIPS** 代表 **联邦信息处理标准（Federal Information Processing Standard）**。

**FIPS 140-2** 是美国政府对加密模块（无论是软件还是硬件）**如何设计、实现和保护**的规定标准。

它是许多行业的**合规要求**，例如：

- 政府机构
- 国防系统
- 医疗行业（如 HIPAA 合规）
- 金融行业（如 PCI DSS 支付卡安全标准）

颁布单位：**NIST（美国国家标准与技术研究院）**

------

#### FIPS 140-2 定义了四个安全等级：

| 等级    | 描述                                               |
| ------- | -------------------------------------------------- |
| Level 1 | 基本加密，允许纯软件加密即可                       |
| Level 2 | 增加防篡改提示（如封条、日志）                     |
| Level 3 | 要求具备防篡改能力、物理安全保护、严格身份认证机制 |
| Level 4 | 极高安全性，能够抵御电压/温度等环境攻击            |

------

### 2️⃣ 什么是 FIPS 140-2 中的 Level 3？

**Level 3** 表示：

- **物理安全性**：设备具有防篡改功能，如果有人试图打开它，密钥会自动被擦除
- **密钥隔离**：不同用户必须经过正确认证才能访问或使用密钥
- **基于角色的访问控制与完整的操作日志**
- 常见于政府、银行、军方系统，以及 **AWS KMS** 等服务

------

### 3️⃣ 什么是 HSM（硬件安全模块）？

🔐 **HSM 是一种物理设备**（通常是独立硬件），用于安全地生成、存储和管理加密密钥。

#### 它的主要特性包括：

- 由硬件强制执行的密钥保护机制
- 硬件加速的加密/解密处理，速度快
- 阻止密钥以明文形式被导出或查看
- 常嵌入在云服务中，如 **AWS KMS、Azure Key Vault 等**



HSM（硬件安全模块）**不是每个服务器机柜都会用到**，它主要应用在对**加密密钥安全要求极高**的场景，比如：

- 政府和军方系统
- 银行、支付网关、金融系统（如 Visa、MasterCard 网络）
- 医疗行业（HIPAA）
- 云服务提供商（像 AWS、Azure、Google Cloud）
- 企业级密钥管理服务（如 AWS KMS、Azure Key Vault）

------

### 📦 HSM 安装位置

- 一般为**专用的硬件设备**，大小类似于**1U/2U 机架式服务器**，可以装进标准的服务器机柜（rack）。
- 有些是插卡形式的（PCIe HSM），可以插在普通服务器上。
- 也有“云 HSM”，比如 AWS CloudHSM，是 AWS 数据中心里的 HSM，你通过网络访问。





------

### ✅ AWS KMS 使用的是通过 FIPS 140-2 Level 3 认证的 HSM

这意味着：

- 你在 AWS KMS 中创建的所有 **CMK（客户主密钥）** 都被安全地保存在**高安全等级、防篡改的硬件设备中**
- **这些主密钥永远不会离开 HSM**，即便是 AWS 工程师也无法接触
- 完全符合多种安全标准与合规要求

------

## 🧠 总结一览表

| 名称           | 含义                              |
| -------------- | --------------------------------- |
| **FIPS 140-2** | 美国政府发布的加密模块安全标准    |
| **Level 3**    | 高级别：防篡改硬件 + 严格身份认证 |
| **HSM**        | 存储并处理加密密钥的高安全性硬件  |
| **AWS KMS**    | 使用 Level 3 HSM 安全保护你的 CMK |



#### S3 CORS

CORS(Cross-Origin Resource Sharing)是S3有的一项功能。其允许网站服务器可以跨区域（**different origin**）调取网页资源，如图片，视频等,其用于保护网页服务器。

origin = scheme (protocol) + hostname (domain) + port

​                            http,https                 example.com    433,5000

在S3上，如果你需要运行静态网站，然后你的网站需要向别的地区的bucket请求资源，可以使用此功能。

启用功能的XML

<CORSConfiguration>
  <CORSRule>
    <AllowedOrigin>*</AllowedOrigin>
    <AllowedMethod>GET</AllowedMethod>
    <AllowedMethod>POST</AllowedMethod>
    <AllowedHeader>*</AllowedHeader>
  </CORSRule>
</CORSConfiguration>



#### S3 MFA Delete

S3上的二重验证功能，如果需要删除bucket里的文件，需要先进行验证，才可以进行操作。只有Root Account可以开启或关闭此功能（使用CLI）,但目前来说，如果你启用了，那么，后续，该bucket的所有文件删除操作都要在CLI进行，因为要在CLI进行MFA验证。